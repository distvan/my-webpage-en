<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="4.2.2">Jekyll</generator><link href="https://www.en.dobrenteiistvan.hu/feed.xml" rel="self" type="application/atom+xml" /><link href="https://www.en.dobrenteiistvan.hu/" rel="alternate" type="text/html" /><updated>2026-08-08T09:11:54+00:00</updated><id>https://www.en.dobrenteiistvan.hu/feed.xml</id><title type="html">Döbrentei István</title><subtitle>Döbrentei István oldala</subtitle><entry><title type="html">Production Agent Architecture: Manage Session State (Part 7)</title><link href="https://www.en.dobrenteiistvan.hu/blog/manage-session-state.html" rel="alternate" type="text/html" title="Production Agent Architecture: Manage Session State (Part 7)" /><published>2026-08-06T00:00:00+00:00</published><updated>2026-08-06T00:00:00+00:00</updated><id>https://www.en.dobrenteiistvan.hu/blog/manage-session-state</id><content type="html" xml:base="https://www.en.dobrenteiistvan.hu/blog/manage-session-state.html"><![CDATA[<p><em>Lessons Learned While Preparing for the Claude Architect Certification</em></p>

<h2 id="the-last-piece-of-domain-1">The last piece of Domain 1</h2>

<p>Every part so far has run inside one process, start to finish: launch the script, the loop does its work, the script ends, nothing survives it. Task 1.7 is about the gap that leaves — what happens when the work spans more than one sitting. You investigate something today, close your laptop, and come back to it tomorrow. Or you reach a point where two different next steps both seem worth trying, and you don’t want either one to interfere with the other.</p>

<p>The example this time is one small file, <code class="language-plaintext highlighter-rouge">cache.py</code>, investigated the way you’d actually investigate something real: not in a single conversation, but across several. A session gets started, closed, picked back up, and split into two directions — and once, the file itself changes in between.</p>

<h2 id="a-session-is-just-a-saved-conversation">A session is just a saved conversation</h2>

<p>Nothing exotic sits behind <code class="language-plaintext highlighter-rouge">--resume &lt;name&gt;</code> or <code class="language-plaintext highlighter-rouge">fork_session</code>. Both are built on one idea: a session is the <code class="language-plaintext highlighter-rouge">messages</code> list the loop has been building up all along, saved somewhere under a name.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">save_session</span><span class="p">(</span><span class="n">name</span><span class="p">:</span> <span class="nb">str</span><span class="p">,</span> <span class="n">messages</span><span class="p">:</span> <span class="nb">list</span><span class="p">[</span><span class="nb">dict</span><span class="p">[</span><span class="nb">str</span><span class="p">,</span> <span class="n">Any</span><span class="p">]])</span> <span class="o">-&gt;</span> <span class="bp">None</span><span class="p">:</span>
    <span class="k">def</span> <span class="nf">as_plain_dict</span><span class="p">(</span><span class="n">block</span><span class="p">:</span> <span class="n">Any</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="n">Any</span><span class="p">:</span>
        <span class="k">if</span> <span class="nb">hasattr</span><span class="p">(</span><span class="n">block</span><span class="p">,</span> <span class="s">"model_dump"</span><span class="p">):</span>
            <span class="k">return</span> <span class="n">block</span><span class="p">.</span><span class="n">model_dump</span><span class="p">()</span>
        <span class="k">raise</span> <span class="nb">TypeError</span><span class="p">(</span><span class="sa">f</span><span class="s">"Not JSON-serializable: </span><span class="si">{</span><span class="n">block</span><span class="si">!r}</span><span class="s">"</span><span class="p">)</span>

    <span class="p">(</span><span class="n">SESSION_DIR</span> <span class="o">/</span> <span class="sa">f</span><span class="s">"</span><span class="si">{</span><span class="n">name</span><span class="si">}</span><span class="s">.json"</span><span class="p">).</span><span class="n">write_text</span><span class="p">(</span><span class="n">json</span><span class="p">.</span><span class="n">dumps</span><span class="p">(</span><span class="n">messages</span><span class="p">,</span> <span class="n">default</span><span class="o">=</span><span class="n">as_plain_dict</span><span class="p">))</span>


<span class="k">def</span> <span class="nf">load_session</span><span class="p">(</span><span class="n">name</span><span class="p">:</span> <span class="nb">str</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">list</span><span class="p">[</span><span class="nb">dict</span><span class="p">[</span><span class="nb">str</span><span class="p">,</span> <span class="n">Any</span><span class="p">]]</span> <span class="o">|</span> <span class="bp">None</span><span class="p">:</span>
    <span class="n">path</span> <span class="o">=</span> <span class="n">SESSION_DIR</span> <span class="o">/</span> <span class="sa">f</span><span class="s">"</span><span class="si">{</span><span class="n">name</span><span class="si">}</span><span class="s">.json"</span>
    <span class="k">return</span> <span class="n">json</span><span class="p">.</span><span class="n">loads</span><span class="p">(</span><span class="n">path</span><span class="p">.</span><span class="n">read_text</span><span class="p">())</span> <span class="k">if</span> <span class="n">path</span><span class="p">.</span><span class="n">exists</span><span class="p">()</span> <span class="k">else</span> <span class="bp">None</span>
</code></pre></div></div>

<p>The one wrinkle is that an assistant turn’s content isn’t plain data — it’s a list of SDK objects (a <code class="language-plaintext highlighter-rouge">TextBlock</code>, a <code class="language-plaintext highlighter-rouge">ToolUseBlock</code>), and <code class="language-plaintext highlighter-rouge">json.dumps</code> doesn’t know what to do with those on its own. <code class="language-plaintext highlighter-rouge">default=as_plain_dict</code> is the fix: whenever the encoder hits something it can’t serialize directly, it calls <code class="language-plaintext highlighter-rouge">.model_dump()</code> on it first. Everything else in <code class="language-plaintext highlighter-rouge">messages</code> — the user turns, the tool results — is already a plain dict and never touches that path.</p>

<p>Because sessions now need to survive as data instead of live SDK objects, the loop itself changes shape slightly from the earlier parts. It takes a starting <code class="language-plaintext highlighter-rouge">messages</code> list instead of building one from a single opening message, and it hands the full updated list back at the end, so the caller has something to save:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">run_agentic_loop</span><span class="p">(</span>
    <span class="n">messages</span><span class="p">:</span> <span class="nb">list</span><span class="p">[</span><span class="nb">dict</span><span class="p">[</span><span class="nb">str</span><span class="p">,</span> <span class="n">Any</span><span class="p">]],</span>
    <span class="n">system</span><span class="p">:</span> <span class="nb">str</span><span class="p">,</span>
    <span class="n">tools</span><span class="p">:</span> <span class="nb">list</span><span class="p">[</span><span class="nb">dict</span><span class="p">[</span><span class="nb">str</span><span class="p">,</span> <span class="n">Any</span><span class="p">]],</span>
    <span class="n">tool_impls</span><span class="p">:</span> <span class="nb">dict</span><span class="p">[</span><span class="nb">str</span><span class="p">,</span> <span class="n">Callable</span><span class="p">[...,</span> <span class="nb">str</span><span class="p">]],</span>
    <span class="n">max_iterations</span><span class="p">:</span> <span class="nb">int</span> <span class="o">=</span> <span class="n">DEFAULT_MAX_ITERATIONS</span><span class="p">,</span>
<span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">tuple</span><span class="p">[</span><span class="nb">str</span><span class="p">,</span> <span class="nb">list</span><span class="p">[</span><span class="nb">dict</span><span class="p">[</span><span class="nb">str</span><span class="p">,</span> <span class="n">Any</span><span class="p">]]]:</span>
    <span class="p">...</span>
</code></pre></div></div>

<p>Everything downstream of this is just two ways of using that list: resume one by name, or copy one under a new name.</p>

<h2 id="resuming-continuing-under-a-name">Resuming: continuing under a name</h2>

<p>There’s deliberately no separate “start a new session” function here. <code class="language-plaintext highlighter-rouge">resume_session</code> handles both cases, because <code class="language-plaintext highlighter-rouge">--resume &lt;name&gt;</code> in the real CLI does too — a name either already has a saved conversation behind it, or it doesn’t, and the same command works either way:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">resume_session</span><span class="p">(</span><span class="n">name</span><span class="p">:</span> <span class="nb">str</span><span class="p">,</span> <span class="n">user_message</span><span class="p">:</span> <span class="nb">str</span><span class="p">,</span> <span class="n">system</span><span class="p">:</span> <span class="nb">str</span><span class="p">,</span> <span class="n">tools</span><span class="p">,</span> <span class="n">tool_impls</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">str</span><span class="p">:</span>
    <span class="n">messages</span> <span class="o">=</span> <span class="n">load_session</span><span class="p">(</span><span class="n">name</span><span class="p">)</span> <span class="ow">or</span> <span class="p">[]</span>
    <span class="n">messages</span><span class="p">.</span><span class="n">append</span><span class="p">({</span><span class="s">"role"</span><span class="p">:</span> <span class="s">"user"</span><span class="p">,</span> <span class="s">"content"</span><span class="p">:</span> <span class="n">user_message</span><span class="p">})</span>
    <span class="n">answer</span><span class="p">,</span> <span class="n">messages</span> <span class="o">=</span> <span class="n">run_agentic_loop</span><span class="p">(</span><span class="n">messages</span><span class="p">,</span> <span class="n">system</span><span class="p">,</span> <span class="n">tools</span><span class="p">,</span> <span class="n">tool_impls</span><span class="p">)</span>
    <span class="n">save_session</span><span class="p">(</span><span class="n">name</span><span class="p">,</span> <span class="n">messages</span><span class="p">)</span>
    <span class="k">return</span> <span class="n">answer</span>
</code></pre></div></div>

<p>Starting a session named <code class="language-plaintext highlighter-rouge">"cache-investigation"</code> and asking it to read <code class="language-plaintext highlighter-rouge">cache.py</code>, then resuming that same name with “is this safe to call from multiple threads at once?”, the second call never touches <code class="language-plaintext highlighter-rouge">read_cache_file</code> again — the file’s contents are already sitting in the saved history from the first call, and the follow-up answers straight from that. That’s the entire value of <code class="language-plaintext highlighter-rouge">--resume &lt;name&gt;</code>: the next work session picks up exactly where the last one left off, without repeating the investigation.</p>

<p>You can find the full file on <a href="https://github.com/distvan/claude-exam-objectives/blob/master/1.7%20Session%20state%2C%20resumption%2C%20and%20forking/session_management_minimal.py">GitHub</a>. A <a href="https://github.com/distvan/claude-exam-objectives/blob/master/1.7%20Session%20state%2C%20resumption%2C%20and%20forking/session_management_minimal.php">PHP port</a> is available too, built the same way as the earlier parts — and slightly simpler on this one point, since every SDK response object in the PHP library already implements <code class="language-plaintext highlighter-rouge">JsonSerializable</code>, so a plain <code class="language-plaintext highlighter-rouge">json_encode($messages)</code> handles the conversion with no extra step at all.</p>

<h2 id="forking-two-branches-one-shared-baseline">Forking: two branches, one shared baseline</h2>

<p><code class="language-plaintext highlighter-rouge">fork_session</code> is even smaller than resuming — it doesn’t run the loop at all, it just copies a saved session under a new name:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">fork_session</span><span class="p">(</span><span class="n">source_name</span><span class="p">:</span> <span class="nb">str</span><span class="p">,</span> <span class="n">new_name</span><span class="p">:</span> <span class="nb">str</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="bp">None</span><span class="p">:</span>
    <span class="n">messages</span> <span class="o">=</span> <span class="n">load_session</span><span class="p">(</span><span class="n">source_name</span><span class="p">)</span>
    <span class="k">if</span> <span class="n">messages</span> <span class="ow">is</span> <span class="bp">None</span><span class="p">:</span>
        <span class="k">raise</span> <span class="nb">ValueError</span><span class="p">(</span><span class="sa">f</span><span class="s">"No saved session named </span><span class="si">{</span><span class="n">source_name</span><span class="si">!r}</span><span class="s">"</span><span class="p">)</span>
    <span class="n">save_session</span><span class="p">(</span><span class="n">new_name</span><span class="p">,</span> <span class="n">messages</span><span class="p">)</span>  <span class="c1"># new_name now has its own copy to diverge from
</span></code></pre></div></div>

<p>Forking <code class="language-plaintext highlighter-rouge">"cache-investigation"</code> into <code class="language-plaintext highlighter-rouge">"cache-investigation-thread-safety"</code> and <code class="language-plaintext highlighter-rouge">"cache-investigation-ttl"</code> gives both names an identical copy of everything established so far — the same file, the same initial analysis. Resuming each one with a different question from there (one about thread safety, one about adding expiration) sends them in genuinely different directions, and neither resume touches the other’s saved file. That’s what makes it a fork instead of two independent investigations: both branches start from work that’s already been done once, instead of paying to redo it twice.</p>

<h2 id="when-resuming-isnt-enough">When resuming isn’t enough</h2>

<p>Resuming assumes the world hasn’t changed since the session was saved. Sometimes it has — the fake <code class="language-plaintext highlighter-rouge">cache.py</code> in this example gets edited partway through, gaining a <code class="language-plaintext highlighter-rouge">threading.Lock</code> it didn’t have when the earlier sessions read it. Asking the resumed session for a recap after that edit is a real test of what “trusting a resumed session” actually costs:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[blind resume, no mention of the edit — outcome depends on the model]
Thread-safety verdict: ❌ Not thread-safe. A race condition exists between
the key check and the cache write...

[informed resume: told exactly what changed]
Yes, the updated cache.py is now thread-safe. The threading.Lock() ensures
that only one thread at a time can execute the critical section...
</code></pre></div></div>

<p>In this run, the blind resume answered from the stale tool result still sitting in its history — the file had already changed, but nothing told the session that, and nothing forced it to check again. That’s not a guaranteed outcome; a model asked a more pointed question, or just choosing to double-check, can and sometimes does re-read the file on its own. That inconsistency is exactly the problem: whether a blind resume gets it right is a gamble on what the model decides to do, not something the code guarantees either way.</p>

<p>The fix removes the gamble instead of hoping to win it. Telling the resumed session directly what changed —</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">resume_session</span><span class="p">(</span>
    <span class="s">"cache-investigation"</span><span class="p">,</span>
    <span class="sa">f</span><span class="s">"cache.py was just edited. Its new contents:</span><span class="se">\n\n</span><span class="si">{</span><span class="n">CACHE_FILE_V2</span><span class="si">}</span><span class="se">\n\n</span><span class="s">"</span>
    <span class="s">"Given this update, is it thread-safe now?"</span><span class="p">,</span>
    <span class="n">SYSTEM_PROMPT</span><span class="p">,</span> <span class="n">TOOLS</span><span class="p">,</span> <span class="n">TOOL_IMPLS</span><span class="p">,</span>
<span class="p">)</span>
</code></pre></div></div>

<p>— means the answer no longer depends on whether the model thinks to re-check anything. The current state is already in the prompt.</p>

<p>For a single file, a short note like that is enough. When a lot has changed since the last session, re-explaining each change piece by piece stops being practical, and a saved session might also be dragging along a long history of tool calls that are no longer relevant to what’s being asked now. That’s when it’s more reliable to stop resuming and start over instead, seeding the fresh session with a short, structured summary of what’s actually still true:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">start_fresh_with_summary</span><span class="p">(</span>
    <span class="s">"Prior finding: cache.py used to have no locking around its dict access. "</span>
    <span class="s">"It has since been edited to wrap that access in a threading.Lock."</span><span class="p">,</span>
    <span class="s">"Is the cache thread-safe now?"</span><span class="p">,</span>
    <span class="n">SYSTEM_PROMPT</span><span class="p">,</span> <span class="n">TOOLS</span><span class="p">,</span> <span class="n">TOOL_IMPLS</span><span class="p">,</span>
<span class="p">)</span>
</code></pre></div></div>

<p>No file on disk, no accumulated tool results from three prior turns — just the one fact that’s still relevant, stated directly. In this run, the fresh-start answer went further than either resume: it confirmed the lock fixes the original race condition, but also noticed that <code class="language-plaintext highlighter-rouge">expensive_lookup</code> runs while the lock is held, which risks serializing every call and could deadlock if that function ever calls back into <code class="language-plaintext highlighter-rouge">get</code>. Nothing about starting fresh caused that extra insight, but nothing about a bloated resumed history was competing for the model’s attention either.</p>

<h2 id="critical-design-decisions">Critical design decisions</h2>

<ul>
  <li><strong>A session is data, not a live object</strong>: saving one means converting SDK response objects into plain dicts first. <code class="language-plaintext highlighter-rouge">save_session</code>’s <code class="language-plaintext highlighter-rouge">default=as_plain_dict</code> is where that conversion happens, once, instead of scattered through the rest of the code.</li>
  <li><strong>The loop hands its state back</strong>: <code class="language-plaintext highlighter-rouge">run_agentic_loop</code> returns the full updated <code class="language-plaintext highlighter-rouge">messages</code> list because something outside it — <code class="language-plaintext highlighter-rouge">resume_session</code> — needs to persist it. A loop that only returns a final answer has nothing left to save.</li>
  <li><strong>One function for “start” and “continue”</strong>: <code class="language-plaintext highlighter-rouge">resume_session</code> doesn’t need a separate creation path. <code class="language-plaintext highlighter-rouge">load_session(name) or []</code> already says everything that distinction would: either there’s a conversation to build on, or there isn’t, and the rest of the function doesn’t care which.</li>
  <li><strong>Forking is a copy, not a merge</strong>: <code class="language-plaintext highlighter-rouge">fork_session</code> just duplicates a saved file under a new name. The branches diverge because they get resumed with different questions afterward, not because forking itself does anything clever.</li>
  <li><strong>Don’t trust a resumed session to notice staleness on its own</strong>: whether it re-checks a changed file is up to the model, not something the code can rely on. Saying what changed, in the resumed prompt itself, is what actually guarantees the answer reflects it.</li>
  <li><strong>Fresh-with-summary scales better than fresh-with-full-history</strong>: a short, curated summary of what’s still true survives a lot of change better than a long resumed history full of tool results that no longer apply.</li>
</ul>

<h2 id="consequences">Consequences</h2>

<ul>
  <li><strong>Treating SDK response objects as if they were already JSON</strong> -&gt; <code class="language-plaintext highlighter-rouge">save_session</code> fails the moment it hits the first <code class="language-plaintext highlighter-rouge">TextBlock</code> or <code class="language-plaintext highlighter-rouge">ToolUseBlock</code>, since neither is serializable without help.</li>
  <li><strong>A loop that never returns its own message history</strong> -&gt; nothing outside it has anything to hand <code class="language-plaintext highlighter-rouge">save_session</code>, so nothing can be resumed later no matter how well the loop itself works.</li>
  <li><strong>Assuming a blind resume will notice a changed file</strong> -&gt; sometimes it does, sometimes it recaps last time’s now-outdated conclusion, and which one happens isn’t something the calling code controls.</li>
  <li><strong>Resuming a long, stale history instead of starting fresh with a summary</strong> -&gt; the model has to sort the parts that still matter from the parts that don’t, using tokens and attention that a short, curated summary wouldn’t have spent at all.</li>
</ul>

<p>That closes out Domain 1. Every part built on the one before it — a loop that could call tools, a coordinator that could delegate, subagents that got exactly the context they needed, workflows that enforced their own rules instead of asking nicely, hooks that intercepted what the tools saw and did, two shapes of decomposition for two shapes of problem, and now, sessions that outlive the process that started them.</p>]]></content><author><name>Istvan Dobrentei</name></author><category term="AI" /><category term="Claude" /><summary type="html"><![CDATA[How a saved session is just the agentic loop's messages list written to disk - resuming a name to continue where it left off, forking a session so two branches share a baseline without redoing work, and why a blind resume can't be trusted to notice a changed file without being told, or restarted fresh with a summary.]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="https://www.en.dobrenteiistvan.hu/assets/images/articles/manage-session-state.svg" /><media:content medium="image" url="https://www.en.dobrenteiistvan.hu/assets/images/articles/manage-session-state.svg" xmlns:media="http://search.yahoo.com/mrss/" /></entry><entry><title type="html">Production Agent Architecture: Task Decomposition Strategies (Part 6)</title><link href="https://www.en.dobrenteiistvan.hu/blog/task-decomposition-strategies.html" rel="alternate" type="text/html" title="Production Agent Architecture: Task Decomposition Strategies (Part 6)" /><published>2026-08-05T00:00:00+00:00</published><updated>2026-08-05T00:00:00+00:00</updated><id>https://www.en.dobrenteiistvan.hu/blog/task-decomposition-strategies</id><content type="html" xml:base="https://www.en.dobrenteiistvan.hu/blog/task-decomposition-strategies.html"><![CDATA[<p><em>Lessons Learned While Preparing for the Claude Architect Certification</em></p>

<h2 id="two-shapes-one-question">Two shapes, one question</h2>

<p>Every part so far has used one shape of loop: give the model a goal and some tools, let it call whatever it wants, keep going until it stops. That shape has a name — dynamic decomposition — but until now the series never had to name it, because there was nothing to contrast it with.</p>

<p>Task 1.6 introduces the contrast. The objective’s own examples are about code review and test-writing, not customer support, so this part switches examples too, the way Part 3 did. The fake project this time is three short files: <code class="language-plaintext highlighter-rouge">auth.py</code>, which checks who’s calling before doing anything, and <code class="language-plaintext highlighter-rouge">orders.py</code> and <code class="language-plaintext highlighter-rouge">payments.py</code>, which don’t. No single file looks broken on its own — the problem only shows up once you compare them.</p>

<p>The question Task 1.6 asks is: given a workflow like that, do you hand the model a goal and let it figure out the steps, or do you write the steps yourself and have the model fill in each one? Both are legitimate. Picking the wrong one for a given job is the actual mistake this task is testing for.</p>

<h2 id="a-fixed-sequence-prompt-chaining">A fixed sequence: prompt chaining</h2>

<p>The first pattern doesn’t use the agentic loop at all. It’s a short Python function that calls the model once per file, then once more at the end. Notice that <code class="language-plaintext highlighter-rouge">review_file</code> takes one file, not all three — that’s deliberate. Pasting every file into a single prompt and asking for one combined review works fine for three short files, but it stops working as the project grows: the more content competes for the model’s attention in one pass, the more likely it is to give some files a thorough read and others a shallow skim. This is what the objective calls attention dilution, and per-file passes sidestep it entirely — no file ever has to share attention with another during its own review.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">review_file</span><span class="p">(</span><span class="n">file_name</span><span class="p">:</span> <span class="nb">str</span><span class="p">,</span> <span class="n">content</span><span class="p">:</span> <span class="nb">str</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">str</span><span class="p">:</span>
    <span class="n">response</span> <span class="o">=</span> <span class="n">client</span><span class="p">.</span><span class="n">messages</span><span class="p">.</span><span class="n">create</span><span class="p">(</span>
        <span class="n">model</span><span class="o">=</span><span class="n">MODEL</span><span class="p">,</span>
        <span class="n">max_tokens</span><span class="o">=</span><span class="mi">400</span><span class="p">,</span>
        <span class="n">system</span><span class="o">=</span><span class="n">REVIEW_SYSTEM_PROMPT</span><span class="p">,</span>
        <span class="n">messages</span><span class="o">=</span><span class="p">[{</span><span class="s">"role"</span><span class="p">:</span> <span class="s">"user"</span><span class="p">,</span> <span class="s">"content"</span><span class="p">:</span> <span class="sa">f</span><span class="s">"File: </span><span class="si">{</span><span class="n">file_name</span><span class="si">}</span><span class="se">\n\n</span><span class="si">{</span><span class="n">content</span><span class="si">}</span><span class="s">"</span><span class="p">}],</span>
    <span class="p">)</span>
    <span class="k">return</span> <span class="n">extract_text</span><span class="p">(</span><span class="n">response</span><span class="p">)</span>


<span class="k">def</span> <span class="nf">run_chained_review</span><span class="p">(</span><span class="n">files</span><span class="p">:</span> <span class="nb">dict</span><span class="p">[</span><span class="nb">str</span><span class="p">,</span> <span class="nb">str</span><span class="p">])</span> <span class="o">-&gt;</span> <span class="nb">tuple</span><span class="p">[</span><span class="nb">dict</span><span class="p">[</span><span class="nb">str</span><span class="p">,</span> <span class="nb">str</span><span class="p">],</span> <span class="nb">str</span><span class="p">]:</span>
    <span class="n">per_file</span> <span class="o">=</span> <span class="p">{</span><span class="n">name</span><span class="p">:</span> <span class="n">review_file</span><span class="p">(</span><span class="n">name</span><span class="p">,</span> <span class="n">content</span><span class="p">)</span> <span class="k">for</span> <span class="n">name</span><span class="p">,</span> <span class="n">content</span> <span class="ow">in</span> <span class="n">files</span><span class="p">.</span><span class="n">items</span><span class="p">()}</span>
    <span class="k">return</span> <span class="n">per_file</span><span class="p">,</span> <span class="n">integrate_findings</span><span class="p">(</span><span class="n">per_file</span><span class="p">)</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">run_chained_review</code> always makes exactly <code class="language-plaintext highlighter-rouge">len(files) + 1</code> calls to the model: one per file, plus one integration pass at the end. That count is decided by the Python code before the first API call ever goes out — the model has no say in how many files get reviewed or when the integration pass happens. That’s the whole definition of prompt chaining: a fixed sequence, written in code, where each step’s output becomes the next step’s input.</p>

<p><code class="language-plaintext highlighter-rouge">integrate_findings</code> is the second half of the chain. It gets every file’s findings at once and looks specifically for patterns that no single-file review could catch:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">integrate_findings</span><span class="p">(</span><span class="n">per_file_findings</span><span class="p">:</span> <span class="nb">dict</span><span class="p">[</span><span class="nb">str</span><span class="p">,</span> <span class="nb">str</span><span class="p">])</span> <span class="o">-&gt;</span> <span class="nb">str</span><span class="p">:</span>
    <span class="n">bundle</span> <span class="o">=</span> <span class="s">"</span><span class="se">\n\n</span><span class="s">"</span><span class="p">.</span><span class="n">join</span><span class="p">(</span><span class="sa">f</span><span class="s">"--- </span><span class="si">{</span><span class="n">name</span><span class="si">}</span><span class="s"> ---</span><span class="se">\n</span><span class="si">{</span><span class="n">findings</span><span class="si">}</span><span class="s">"</span> <span class="k">for</span> <span class="n">name</span><span class="p">,</span> <span class="n">findings</span> <span class="ow">in</span> <span class="n">per_file_findings</span><span class="p">.</span><span class="n">items</span><span class="p">())</span>
    <span class="n">response</span> <span class="o">=</span> <span class="n">client</span><span class="p">.</span><span class="n">messages</span><span class="p">.</span><span class="n">create</span><span class="p">(</span>
        <span class="n">model</span><span class="o">=</span><span class="n">MODEL</span><span class="p">,</span> <span class="n">max_tokens</span><span class="o">=</span><span class="mi">400</span><span class="p">,</span> <span class="n">system</span><span class="o">=</span><span class="n">INTEGRATION_SYSTEM_PROMPT</span><span class="p">,</span> <span class="n">messages</span><span class="o">=</span><span class="p">[{</span><span class="s">"role"</span><span class="p">:</span> <span class="s">"user"</span><span class="p">,</span> <span class="s">"content"</span><span class="p">:</span> <span class="n">bundle</span><span class="p">}]</span>
    <span class="p">)</span>
    <span class="k">return</span> <span class="n">extract_text</span><span class="p">(</span><span class="n">response</span><span class="p">)</span>
</code></pre></div></div>

<p>Both functions end the same way: pull the text block out of the response and return it. That’s common enough between them — and with the loop in Pattern 2 below — that it’s its own small function, <code class="language-plaintext highlighter-rouge">extract_text</code>, instead of being repeated three times.</p>

<p>Running this against the three-file project, the per-file pass flagged <code class="language-plaintext highlighter-rouge">payments.py</code> for missing an amount check and <code class="language-plaintext highlighter-rouge">orders.py</code> and <code class="language-plaintext highlighter-rouge">auth.py</code> for their own local issues — each file reviewed with no idea the others exist. The integration pass then caught something none of them could: <code class="language-plaintext highlighter-rouge">auth.py</code>’s review flagged a missing <code class="language-plaintext highlighter-rouge">try/except</code> around its database call, and the integration pass pointed out that <code class="language-plaintext highlighter-rouge">orders.py</code> and <code class="language-plaintext highlighter-rouge">payments.py</code> make the same kind of database call with no such flag anywhere in their own reviews — an inconsistency that’s only visible once you can see all three findings side by side.</p>

<h2 id="no-fixed-sequence-dynamic-decomposition">No fixed sequence: dynamic decomposition</h2>

<p>The second pattern is the agentic loop from Part 1, unchanged, pointed at a more open-ended goal: figure out what to test first in a codebase you haven’t seen yet.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">DYNAMIC_SYSTEM_PROMPT</span> <span class="o">=</span> <span class="p">(</span>
    <span class="s">"You are planning test coverage for a legacy codebase. Use list_files and "</span>
    <span class="s">"read_file to explore it. Decide yourself which files are worth reading "</span>
    <span class="s">"and in what order — nothing here fixes that in advance. When you're "</span>
    <span class="s">"done, give a prioritized list of what to test first and why."</span>
<span class="p">)</span>


<span class="k">def</span> <span class="nf">run_dynamic_investigation</span><span class="p">(</span><span class="n">goal</span><span class="p">:</span> <span class="nb">str</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">str</span><span class="p">:</span>
    <span class="k">return</span> <span class="n">run_agentic_loop</span><span class="p">(</span>
        <span class="n">user_message</span><span class="o">=</span><span class="n">goal</span><span class="p">,</span>
        <span class="n">system</span><span class="o">=</span><span class="n">DYNAMIC_SYSTEM_PROMPT</span><span class="p">,</span>
        <span class="n">tools</span><span class="o">=</span><span class="n">DYNAMIC_TOOLS</span><span class="p">,</span>
        <span class="n">tool_impls</span><span class="o">=</span><span class="n">DYNAMIC_TOOL_IMPLS</span><span class="p">,</span>
    <span class="p">)</span>
</code></pre></div></div>

<p>Nothing in this code decides how many times <code class="language-plaintext highlighter-rouge">read_file</code> gets called, or in what order. On an actual run, the model listed the files, read all three, and ranked <code class="language-plaintext highlighter-rouge">payments.py</code> first — not because a fixed rule said “financial code first,” but because reading it surfaced two separate problems at once (no caller check, no amount check), which made it the most convincing candidate for “test this before anything else” once the model could see both issues together. A fixed pipeline could easily have been written to always review files in the same order, but it couldn’t have produced that specific reasoning without first reading the file and discovering what was actually wrong with it. That’s what “adapts as dependencies are discovered” means in practice: the plan is a consequence of what got found, not a template filled in afterward.</p>

<p>You can find the full file on <a href="https://github.com/distvan/claude-exam-objectives/blob/master/1.6%20Task%20decomposition%20strategies/task_decomposition_minimal.py">GitHub</a>. A <a href="https://github.com/distvan/claude-exam-objectives/blob/master/1.6%20Task%20decomposition%20strategies/task_decomposition_minimal.php">PHP port</a> is available too, built the same way as the earlier parts.</p>

<h2 id="picking-one-over-the-other">Picking one over the other</h2>

<p>The three files in this example are small enough that either pattern could technically handle both jobs. The point isn’t that one pattern is better — it’s that they fit different shapes of problem, and the shape of the problem is usually obvious before you write a line of code.</p>

<p>A code review over a known, fixed set of files is predictable: you already know how many files there are, and every one of them deserves the same kind of local pass before anything looks at all of them together. That predictability is exactly what a fixed pipeline is good at — it guarantees every file gets reviewed, once, in isolation, with no risk of the model deciding one file doesn’t need a look.</p>

<p>“What should we test first in a codebase we don’t know yet” isn’t predictable the same way. There’s no fixed number of files worth reading, and which ones matter depends entirely on what’s in them — something you can’t know until you’ve looked. Forcing that into a fixed sequence means either reading every file whether it’s relevant or not, or guessing at relevance before you have any evidence for the guess. Letting the model decide, one <code class="language-plaintext highlighter-rouge">read_file</code> call at a time, means it only goes deeper where the code actually gives it a reason to.</p>

<h2 id="critical-design-decisions">Critical design decisions</h2>

<ul>
  <li><strong>The step count is the tell</strong>: if you can write down the exact number and order of steps before running anything, that’s a sign the job wants prompt chaining. If the right number of steps depends on what the first few steps find, that’s dynamic decomposition.</li>
  <li><strong>Isolation is a feature, not a limitation</strong>: <code class="language-plaintext highlighter-rouge">review_file</code>’s system prompt explicitly tells it not to comment on other files. A per-file pass that tries to reason about the whole project defeats the purpose of splitting the work up in the first place.</li>
  <li><strong>The integration pass needs everything, not a summary</strong>: <code class="language-plaintext highlighter-rouge">integrate_findings</code> receives every file’s findings verbatim. A cross-file inconsistency is easy to miss if one side of it got paraphrased away before the comparison happened.</li>
  <li><strong>Dynamic decomposition is just the ordinary agentic loop</strong>: nothing about <code class="language-plaintext highlighter-rouge">run_dynamic_investigation</code> is new. Task 1.6 isn’t asking you to build a different mechanism — it’s asking you to recognize when the mechanism you already have is the right choice, and when it isn’t.</li>
</ul>

<h2 id="consequences">Consequences</h2>

<ul>
  <li><strong>Forcing an open-ended investigation into a fixed pipeline</strong> -&gt; either every file gets the same depth of attention whether it needs it or not, or someone has to guess in advance which files matter, before any evidence exists to base that guess on.</li>
  <li><strong>Letting a predictable review run as an open-ended investigation</strong> -&gt; the model might decide one file doesn’t need a look, or spend disproportionate effort on one file while skimming another, since nothing guarantees uniform coverage.</li>
  <li><strong>A per-file pass that isn’t told to stay isolated</strong> -&gt; it starts reasoning about files it hasn’t actually read yet, producing cross-file claims that are really just guesses dressed up as findings.</li>
  <li><strong>An integration pass fed summaries instead of full findings</strong> -&gt; the one thing it exists to catch — inconsistency between files — is exactly what a summary is most likely to smooth over.</li>
</ul>

<p>The two patterns aren’t competitors. A production review pipeline can run prompt chaining for the parts of the job that are genuinely predictable, and drop into dynamic decomposition the moment a step’s next move actually depends on what it just found.</p>]]></content><author><name>Istvan Dobrentei</name></author><category term="AI" /><category term="Claude" /><summary type="html"><![CDATA[Prompt chaining versus dynamic decomposition - a fixed per-file review pipeline that avoids attention dilution, an integration pass that catches cross-file drift no single review can, an agentic loop that decides its own step count while exploring an unfamiliar codebase, and how the shape of the problem tells you which pattern fits.]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="https://www.en.dobrenteiistvan.hu/assets/images/articles/task-decomposition-strategies.svg" /><media:content medium="image" url="https://www.en.dobrenteiistvan.hu/assets/images/articles/task-decomposition-strategies.svg" xmlns:media="http://search.yahoo.com/mrss/" /></entry><entry><title type="html">Production Agent Architecture: Agent SDK Hooks (Part 5)</title><link href="https://www.en.dobrenteiistvan.hu/blog/agent-sdk-hooks.html" rel="alternate" type="text/html" title="Production Agent Architecture: Agent SDK Hooks (Part 5)" /><published>2026-08-04T00:00:00+00:00</published><updated>2026-08-04T00:00:00+00:00</updated><id>https://www.en.dobrenteiistvan.hu/blog/agent-sdk-hooks</id><content type="html" xml:base="https://www.en.dobrenteiistvan.hu/blog/agent-sdk-hooks.html"><![CDATA[<p><em>Lessons Learned While Preparing for the Claude Architect Certification</em></p>

<h2 id="picking-up-where-part-4-left-off">Picking up where Part 4 left off</h2>

<p>Part 4 built a prerequisite gate straight into <code class="language-plaintext highlighter-rouge">process_refund</code>’s own code, and closed with a promise: the Agent SDK has a dedicated mechanism for this same kind of check, called hooks, and they deserved their own part. This is that part.</p>

<p>The objective for Task 1.5 asks about two things that sound different but solve the same underlying problem — a tool result or a tool call passing through untouched when it shouldn’t. The first is interception: stopping a tool call before it runs, the way Part 4’s gate stopped an unverified refund. The second is normalization: cleaning up a tool’s result after it runs, before the model ever reads it. Both are described as hooks — <code class="language-plaintext highlighter-rouge">PreToolUse</code> for the first, <code class="language-plaintext highlighter-rouge">PostToolUse</code> for the second — and both exist so that a rule doesn’t have to live inside the tool it applies to, or inside the model’s head.</p>

<p>The example keeps the same three tools this agent actually uses on a given case — <code class="language-plaintext highlighter-rouge">lookup_order</code>, <code class="language-plaintext highlighter-rouge">process_refund</code>, <code class="language-plaintext highlighter-rouge">escalate_to_human</code> — but drops Part 4’s customer-verification step. That step is real and worth keeping in a production agent, but it’s a different lesson (a prerequisite gate, not a hook), and dragging it along here would only add a <code class="language-plaintext highlighter-rouge">customer_id</code> to thread through every function without teaching anything new about hooks. What’s left is small enough to hold in your head in one read: two tools that do real work, one that hands a case to a person, and two hooks watching the traffic between the model and all three.</p>

<h2 id="two-hook-points-in-one-loop">Two hook points in one loop</h2>

<p>Every part so far reused the same agentic loop, with small additions each time. This part’s addition is two lists of functions the loop consults on every tool call: <code class="language-plaintext highlighter-rouge">pre_tool_hooks</code>, checked before the real tool runs, and <code class="language-plaintext highlighter-rouge">post_tool_hooks</code>, checked after:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">for</span> <span class="n">block</span> <span class="ow">in</span> <span class="n">calls</span><span class="p">:</span>
    <span class="n">blocked</span> <span class="o">=</span> <span class="bp">None</span>
    <span class="k">for</span> <span class="n">hook</span> <span class="ow">in</span> <span class="n">pre_tool_hooks</span><span class="p">:</span>
        <span class="n">blocked</span> <span class="o">=</span> <span class="n">hook</span><span class="p">(</span><span class="n">block</span><span class="p">.</span><span class="n">name</span><span class="p">,</span> <span class="n">block</span><span class="p">.</span><span class="nb">input</span><span class="p">)</span>
        <span class="k">if</span> <span class="n">blocked</span> <span class="ow">is</span> <span class="ow">not</span> <span class="bp">None</span><span class="p">:</span>
            <span class="k">break</span>

    <span class="k">if</span> <span class="n">blocked</span> <span class="ow">is</span> <span class="ow">not</span> <span class="bp">None</span><span class="p">:</span>
        <span class="n">result</span> <span class="o">=</span> <span class="n">blocked</span>
    <span class="k">else</span><span class="p">:</span>
        <span class="n">result</span> <span class="o">=</span> <span class="n">tool_impls</span><span class="p">[</span><span class="n">block</span><span class="p">.</span><span class="n">name</span><span class="p">](</span><span class="o">**</span><span class="n">block</span><span class="p">.</span><span class="nb">input</span><span class="p">)</span>
        <span class="k">for</span> <span class="n">hook</span> <span class="ow">in</span> <span class="n">post_tool_hooks</span><span class="p">:</span>
            <span class="n">result</span> <span class="o">=</span> <span class="n">hook</span><span class="p">(</span><span class="n">block</span><span class="p">.</span><span class="n">name</span><span class="p">,</span> <span class="n">block</span><span class="p">.</span><span class="nb">input</span><span class="p">,</span> <span class="n">result</span><span class="p">)</span>
</code></pre></div></div>

<p>A pre-tool hook takes the tool’s name and its input, and returns either <code class="language-plaintext highlighter-rouge">None</code> — “no objection, proceed” — or a string that replaces the entire call. When that happens, <code class="language-plaintext highlighter-rouge">tool_impls[block.name]</code> never runs at all. A post-tool hook takes the name, the input, and whatever the tool actually returned, and hands back a (possibly rewritten) result. This is a hand-rolled stand-in for what the real Agent SDK calls <code class="language-plaintext highlighter-rouge">PreToolUse</code> and <code class="language-plaintext highlighter-rouge">PostToolUse</code>, small enough to read in one pass, but it draws the exact same line: interception happens before the call, normalization happens after it.</p>

<p>You can find the full file on <a href="https://github.com/distvan/claude-exam-objectives/blob/master/1.5%20Agent%20SDK%20hooks/agent_sdk_hooks_minimal.py">GitHub</a>. A <a href="https://github.com/distvan/claude-exam-objectives/blob/master/1.5%20Agent%20SDK%20hooks/agent_sdk_hooks_minimal.php">PHP port</a> is available too, built the same way as the earlier parts.</p>

<h2 id="blocking-a-call-the-tool-never-sees">Blocking a call the tool never sees</h2>

<p>In this example, <code class="language-plaintext highlighter-rouge">process_refund</code> itself contains no policy logic at all — no threshold, no check of any kind. It trusts its inputs and does the one thing it’s for:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">process_refund</span><span class="p">(</span><span class="n">order_id</span><span class="p">:</span> <span class="nb">str</span><span class="p">,</span> <span class="n">amount</span><span class="p">:</span> <span class="nb">float</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">str</span><span class="p">:</span>
    <span class="k">if</span> <span class="n">order_id</span> <span class="ow">not</span> <span class="ow">in</span> <span class="n">ORDERS</span><span class="p">:</span>
        <span class="k">return</span> <span class="sa">f</span><span class="s">"ERROR: order </span><span class="si">{</span><span class="n">order_id</span><span class="si">}</span><span class="s"> not found. retryable=false"</span>
    <span class="k">return</span> <span class="sa">f</span><span class="s">"Refunded $</span><span class="si">{</span><span class="n">amount</span><span class="si">:</span><span class="p">.</span><span class="mi">2</span><span class="n">f</span><span class="si">}</span><span class="s"> for order </span><span class="si">{</span><span class="n">order_id</span><span class="si">}</span><span class="s">."</span>
</code></pre></div></div>

<p>Every bit of the $500 rule lives somewhere else entirely, in a hook that <code class="language-plaintext highlighter-rouge">process_refund</code> never calls and doesn’t know exists:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">REFUND_THRESHOLD</span> <span class="o">=</span> <span class="mf">500.00</span>

<span class="k">def</span> <span class="nf">block_large_refunds</span><span class="p">(</span><span class="n">tool_name</span><span class="p">:</span> <span class="nb">str</span><span class="p">,</span> <span class="n">tool_input</span><span class="p">:</span> <span class="nb">dict</span><span class="p">[</span><span class="nb">str</span><span class="p">,</span> <span class="n">Any</span><span class="p">])</span> <span class="o">-&gt;</span> <span class="nb">str</span> <span class="o">|</span> <span class="bp">None</span><span class="p">:</span>
    <span class="k">if</span> <span class="n">tool_name</span> <span class="o">!=</span> <span class="s">"process_refund"</span><span class="p">:</span>
        <span class="k">return</span> <span class="bp">None</span>
    <span class="n">amount</span> <span class="o">=</span> <span class="n">tool_input</span><span class="p">.</span><span class="n">get</span><span class="p">(</span><span class="s">"amount"</span><span class="p">,</span> <span class="mi">0</span><span class="p">)</span>
    <span class="k">if</span> <span class="n">amount</span> <span class="o">&lt;=</span> <span class="n">REFUND_THRESHOLD</span><span class="p">:</span>
        <span class="k">return</span> <span class="bp">None</span>
    <span class="k">return</span> <span class="p">(</span>
        <span class="sa">f</span><span class="s">"BLOCKED by policy hook: a refund of $</span><span class="si">{</span><span class="n">amount</span><span class="si">:</span><span class="p">.</span><span class="mi">2</span><span class="n">f</span><span class="si">}</span><span class="s"> exceeds the "</span>
        <span class="sa">f</span><span class="s">"$</span><span class="si">{</span><span class="n">REFUND_THRESHOLD</span><span class="si">:</span><span class="p">.</span><span class="mi">2</span><span class="n">f</span><span class="si">}</span><span class="s"> threshold this agent may approve on its "</span>
        <span class="s">"own. Call escalate_to_human instead."</span>
    <span class="p">)</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">block_large_refunds</code> reads <code class="language-plaintext highlighter-rouge">tool_name</code> and <code class="language-plaintext highlighter-rouge">tool_input</code> the same way every hook does — it has no special access to <code class="language-plaintext highlighter-rouge">process_refund</code>’s internals, because there’s nothing there to access. Compare that to Part 4’s gate, which lived inside <code class="language-plaintext highlighter-rouge">process_refund</code> itself: a check written that way only ever protects the one tool it’s written into, and changing the rule means editing that tool’s code. A hook sits one layer above every tool call instead, so raising the threshold to $750, or adding a second rule for a completely different tool, never touches <code class="language-plaintext highlighter-rouge">process_refund</code> at all.</p>

<p><code class="language-plaintext highlighter-rouge">demo_hook_without_model()</code> proves this the same way Part 4’s gate demo did — by calling the hook directly, with no model and no loop involved:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">demo_hook_without_model</span><span class="p">()</span> <span class="o">-&gt;</span> <span class="bp">None</span><span class="p">:</span>
    <span class="n">big</span> <span class="o">=</span> <span class="p">{</span><span class="s">"order_id"</span><span class="p">:</span> <span class="s">"A123"</span><span class="p">,</span> <span class="s">"amount"</span><span class="p">:</span> <span class="mf">900.00</span><span class="p">}</span>
    <span class="n">small</span> <span class="o">=</span> <span class="p">{</span><span class="s">"order_id"</span><span class="p">:</span> <span class="s">"A123"</span><span class="p">,</span> <span class="s">"amount"</span><span class="p">:</span> <span class="mf">40.00</span><span class="p">}</span>
    <span class="k">print</span><span class="p">(</span><span class="s">"large refund:"</span><span class="p">,</span> <span class="n">block_large_refunds</span><span class="p">(</span><span class="s">"process_refund"</span><span class="p">,</span> <span class="n">big</span><span class="p">))</span>
    <span class="k">print</span><span class="p">(</span><span class="s">"small refund:"</span><span class="p">,</span> <span class="n">block_large_refunds</span><span class="p">(</span><span class="s">"process_refund"</span><span class="p">,</span> <span class="n">small</span><span class="p">))</span>
    <span class="k">print</span><span class="p">(</span><span class="s">"different tool:"</span><span class="p">,</span> <span class="n">block_large_refunds</span><span class="p">(</span><span class="s">"lookup_order"</span><span class="p">,</span> <span class="n">big</span><span class="p">))</span>
</code></pre></div></div>

<p>The $900 request comes back blocked. The $40 request and the unrelated <code class="language-plaintext highlighter-rouge">lookup_order</code> call both come back <code class="language-plaintext highlighter-rouge">None</code> — the hook only ever has an opinion about <code class="language-plaintext highlighter-rouge">process_refund</code> calls above its threshold, and stays out of the way for everything else.</p>

<p>For comparison, the example project also keeps the prompt-only version of this exact rule around, the same way Part 4 did:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">PROMPT_ONLY_REFUND_POLICY</span> <span class="o">=</span> <span class="p">(</span>
    <span class="s">"Never approve a refund over $500 on your own — always escalate "</span>
    <span class="s">"those to a human agent instead of calling process_refund."</span>
<span class="p">)</span>
</code></pre></div></div>

<p>This sentence would genuinely help, most of the time — the same as Part 4’s prompt-only version helped, most of the time. It has the same ceiling, too: nothing forces the model to reread it before every single refund decision, and a threshold that protects money deserves better than “most of the time.”</p>

<h2 id="one-shape-out-of-two-backends">One shape out of two backends</h2>

<p>The second hook solves a different problem. <code class="language-plaintext highlighter-rouge">lookup_order</code> in this example pulls from two backends that describe an order differently — a legacy system using a numeric status code and a Unix timestamp, and a modern one using a status word and an ISO 8601 date:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">ORDERS</span> <span class="o">=</span> <span class="p">{</span>
    <span class="s">"A123"</span><span class="p">:</span> <span class="p">{</span><span class="s">"status_code"</span><span class="p">:</span> <span class="mi">2</span><span class="p">,</span> <span class="s">"shipped_at_unix"</span><span class="p">:</span> <span class="mi">1750000000</span><span class="p">,</span> <span class="p">...},</span>   <span class="c1"># legacy backend
</span>    <span class="s">"B456"</span><span class="p">:</span> <span class="p">{</span><span class="s">"status"</span><span class="p">:</span> <span class="s">"delivered"</span><span class="p">,</span> <span class="s">"shipped_at_iso"</span><span class="p">:</span> <span class="s">"2025-06-20T10:00:00Z"</span><span class="p">,</span> <span class="p">...},</span>  <span class="c1"># modern backend
</span><span class="p">}</span>
</code></pre></div></div>

<p>Nothing about which backend answers is under the model’s control, and nothing about it should have to be. Without a hook, the model would have to learn both shapes, recognize which one it’s looking at on every single order, and hope it never confuses <code class="language-plaintext highlighter-rouge">2</code> for “delivered” with <code class="language-plaintext highlighter-rouge">2</code> meaning something else in a different response. A <code class="language-plaintext highlighter-rouge">PostToolUse</code> hook removes that entirely by converting both shapes into one, before either ever reaches the model:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">normalize_order_result</span><span class="p">(</span><span class="n">tool_name</span><span class="p">:</span> <span class="nb">str</span><span class="p">,</span> <span class="n">tool_input</span><span class="p">:</span> <span class="nb">dict</span><span class="p">[</span><span class="nb">str</span><span class="p">,</span> <span class="n">Any</span><span class="p">],</span> <span class="n">raw_result</span><span class="p">:</span> <span class="nb">str</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">str</span><span class="p">:</span>
    <span class="k">if</span> <span class="n">tool_name</span> <span class="o">!=</span> <span class="s">"lookup_order"</span> <span class="ow">or</span> <span class="ow">not</span> <span class="n">raw_result</span><span class="p">.</span><span class="n">startswith</span><span class="p">(</span><span class="s">"{"</span><span class="p">):</span>
        <span class="k">return</span> <span class="n">raw_result</span>

    <span class="n">order</span> <span class="o">=</span> <span class="n">json</span><span class="p">.</span><span class="n">loads</span><span class="p">(</span><span class="n">raw_result</span><span class="p">)</span>
    <span class="k">if</span> <span class="s">"status_code"</span> <span class="ow">in</span> <span class="n">order</span><span class="p">:</span>
        <span class="n">status</span> <span class="o">=</span> <span class="n">STATUS_CODE_MAP</span><span class="p">[</span><span class="n">order</span><span class="p">[</span><span class="s">"status_code"</span><span class="p">]]</span>
        <span class="n">date</span> <span class="o">=</span> <span class="n">datetime</span><span class="p">.</span><span class="n">fromtimestamp</span><span class="p">(</span><span class="n">order</span><span class="p">[</span><span class="s">"shipped_at_unix"</span><span class="p">],</span> <span class="n">tz</span><span class="o">=</span><span class="n">timezone</span><span class="p">.</span><span class="n">utc</span><span class="p">).</span><span class="n">date</span><span class="p">().</span><span class="n">isoformat</span><span class="p">()</span>
    <span class="k">else</span><span class="p">:</span>
        <span class="n">status</span> <span class="o">=</span> <span class="n">order</span><span class="p">[</span><span class="s">"status"</span><span class="p">]</span>
        <span class="n">date</span> <span class="o">=</span> <span class="n">order</span><span class="p">[</span><span class="s">"shipped_at_iso"</span><span class="p">][:</span><span class="mi">10</span><span class="p">]</span>
    <span class="k">return</span> <span class="sa">f</span><span class="s">"status=</span><span class="si">{</span><span class="n">status</span><span class="si">}</span><span class="s"> date=</span><span class="si">{</span><span class="n">date</span><span class="si">}</span><span class="s"> amount=$</span><span class="si">{</span><span class="n">order</span><span class="p">[</span><span class="s">'amount'</span><span class="p">]</span><span class="si">:</span><span class="p">.</span><span class="mi">2</span><span class="n">f</span><span class="si">}</span><span class="s">"</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">demo_normalization()</code> runs both raw results through this hook and prints them side by side. Both come out as <code class="language-plaintext highlighter-rouge">status=delivered date=... amount=$...</code> — same three fields, same format, regardless of which backend produced the original response. The error strings <code class="language-plaintext highlighter-rouge">lookup_order</code> returns for a missing order pass straight through untouched, since they don’t start with <code class="language-plaintext highlighter-rouge">{</code>; a hook that tries to normalize everything indiscriminately usually ends up breaking the one shape it wasn’t expecting.</p>

<h2 id="critical-design-decisions">Critical design decisions</h2>

<ul>
  <li><strong>Interception happens before the tool runs, normalization after</strong>: a <code class="language-plaintext highlighter-rouge">PreToolUse</code> hook can stop a call outright; a <code class="language-plaintext highlighter-rouge">PostToolUse</code> hook only ever gets to reshape a result the tool already produced.</li>
  <li><strong>A hook doesn’t require touching the tool it governs</strong>: <code class="language-plaintext highlighter-rouge">block_large_refunds</code> never edits <code class="language-plaintext highlighter-rouge">process_refund</code>, and <code class="language-plaintext highlighter-rouge">process_refund</code> never mentions the threshold. That decoupling is the whole reason to reach for a hook instead of another in-function check.</li>
  <li><strong><code class="language-plaintext highlighter-rouge">None</code> means “no objection,” not “nothing happened”</strong>: a pre-tool hook has to actively return a replacement to block anything. Anything it doesn’t recognize passes straight through.</li>
  <li><strong>Test a hook the same way you test a gate</strong>: <code class="language-plaintext highlighter-rouge">demo_hook_without_model()</code> calls <code class="language-plaintext highlighter-rouge">block_large_refunds</code> directly, no model or loop required, for the same reason Part 4 tested its gate that way — the guarantee shouldn’t depend on the model cooperating.</li>
  <li><strong>Normalize only what you recognize</strong>: <code class="language-plaintext highlighter-rouge">normalize_order_result</code> checks the shape of what it received before touching it, so error strings and anything unexpected pass through unchanged instead of crashing on a shape the hook wasn’t written for.</li>
</ul>

<h2 id="consequences">Consequences</h2>

<ul>
  <li><strong>A business rule written inside one tool</strong> -&gt; protecting a second tool with the same rule means copying the check, and every copy can drift out of sync with the others. A hook that inspects <code class="language-plaintext highlighter-rouge">tool_name</code> applies the same rule everywhere it’s registered, from one place.</li>
  <li><strong>A prompt-only threshold on a financial operation</strong> -&gt; the same non-zero failure rate Part 4 described, just applied to a dollar amount instead of an identity check.</li>
  <li><strong>No normalization hook, two backends, one model</strong> -&gt; the model has to hold both formats in its head and guess correctly every time, and a status code that means one thing in one response can silently mean something else if a third backend joins later with its own numbering.</li>
  <li><strong>A hook that transforms without checking the shape first</strong> -&gt; the moment an error string or an unexpected format reaches it, it either crashes or silently mangles something it was never meant to touch.</li>
</ul>

<p>Interception and normalization are really the same idea pointed in two directions: a layer between the model and a tool that gets to see every call and every result before anyone else does, and can act on what it sees without either side needing to know it’s there.</p>]]></content><author><name>Istvan Dobrentei</name></author><category term="AI" /><category term="Claude" /><summary type="html"><![CDATA[Why a policy rule belongs in a hook instead of inside the tool it governs - PreToolUse interception that blocks a call before it runs, PostToolUse normalization that reshapes a tool's result before the model reads it, testing a hook without a model in the loop, and why returning None means no objection rather than nothing happened.]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="https://www.en.dobrenteiistvan.hu/assets/images/articles/agent-sdk-hooks.svg" /><media:content medium="image" url="https://www.en.dobrenteiistvan.hu/assets/images/articles/agent-sdk-hooks.svg" xmlns:media="http://search.yahoo.com/mrss/" /></entry><entry><title type="html">Production Agent Architecture: Multi Step Workflows (Part 4)</title><link href="https://www.en.dobrenteiistvan.hu/blog/multi-step-workflows.html" rel="alternate" type="text/html" title="Production Agent Architecture: Multi Step Workflows (Part 4)" /><published>2026-08-03T00:00:00+00:00</published><updated>2026-08-03T00:00:00+00:00</updated><id>https://www.en.dobrenteiistvan.hu/blog/multi-step-workflows</id><content type="html" xml:base="https://www.en.dobrenteiistvan.hu/blog/multi-step-workflows.html"><![CDATA[<p><em>Lessons Learned While Preparing for the Claude Architect Certification</em></p>

<h2 id="back-to-customer-support-on-purpose">Back to customer support, on purpose</h2>

<p>Part 3 moved to a research pipeline because that objective’s own examples were about research subagents. This part moves back to customer support, for the same reason: the official objective for Task 1.4 is written around a refund flow. It talks about verifying a customer before a refund, about escalating a case a bot can’t finish alone, and about giving a human agent a full picture instead of a raw chat log to dig through. The exam guide’s own sample question describes the exact bug this part fixes: an agent that skips customer verification in 12% of cases and occasionally refunds the wrong account.</p>

<p>So the example this time is a single support agent again, close to the one Part 2 started with — but with a question none of the earlier parts asked: what happens between two tool calls that absolutely must happen in the right order, and what happens when the agent can’t finish the job by itself?</p>

<h2 id="programmatic-enforcement-vs-asking-nicely">Programmatic enforcement vs. asking nicely</h2>

<p>Every part so far had a system prompt telling the model what to do. That’s prompt-based guidance, and it works well for most decisions — which subagent fits a question, how many sources to check. It stops being good enough the moment a step protects something with a financial consequence, like refunding money without knowing who the customer actually is.</p>

<p>The reason isn’t that the model is careless. A prompt instruction is probabilistic by nature. “Always verify the customer first” competes with everything else in the prompt and the conversation for the model’s attention on any given turn, and on some fraction of turns, it loses. A 12% skip rate isn’t a broken model — it’s what a probabilistic instruction protecting a critical step looks like at scale.</p>

<p>The fix is to stop asking and start checking. In the example project, <code class="language-plaintext highlighter-rouge">process_refund</code> doesn’t trust that <code class="language-plaintext highlighter-rouge">get_customer</code> ran earlier in the conversation. It checks a <code class="language-plaintext highlighter-rouge">verified_customers</code> set directly, at the top of the function, before doing anything else:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">process_refund</span><span class="p">(</span><span class="n">customer_id</span><span class="p">:</span> <span class="nb">str</span><span class="p">,</span> <span class="n">order_id</span><span class="p">:</span> <span class="nb">str</span><span class="p">,</span> <span class="n">amount</span><span class="p">:</span> <span class="nb">float</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">str</span><span class="p">:</span>
    <span class="k">if</span> <span class="n">customer_id</span> <span class="ow">not</span> <span class="ow">in</span> <span class="n">verified_customers</span><span class="p">:</span>
        <span class="k">return</span> <span class="p">(</span>
            <span class="s">"ERROR: prerequisite not met. process_refund requires a "</span>
            <span class="s">"verified customer_id from get_customer first. retryable=false"</span>
        <span class="p">)</span>
    <span class="p">...</span>
</code></pre></div></div>

<p>This is what “programmatic prerequisite” means: a gate in the code path, not a line in a prompt. It doesn’t matter whether the model forgot to call <code class="language-plaintext highlighter-rouge">get_customer</code>, whether a bug somewhere skipped it, or whether nobody went through the model at all. The example project shows that last case directly — <code class="language-plaintext highlighter-rouge">demo_prerequisite_gate()</code> calls <code class="language-plaintext highlighter-rouge">process_refund</code> straight from Python, with no agentic loop in between, and the gate still blocks it:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">demo_prerequisite_gate</span><span class="p">()</span> <span class="o">-&gt;</span> <span class="bp">None</span><span class="p">:</span>
    <span class="k">print</span><span class="p">(</span><span class="n">process_refund</span><span class="p">(</span><span class="n">customer_id</span><span class="o">=</span><span class="s">"cust_001"</span><span class="p">,</span> <span class="n">order_id</span><span class="o">=</span><span class="s">"A123"</span><span class="p">,</span> <span class="n">amount</span><span class="o">=</span><span class="mf">40.00</span><span class="p">))</span>
    <span class="n">get_customer</span><span class="p">(</span><span class="s">"alice@example.com"</span><span class="p">)</span>
    <span class="k">print</span><span class="p">(</span><span class="n">process_refund</span><span class="p">(</span><span class="n">customer_id</span><span class="o">=</span><span class="s">"cust_001"</span><span class="p">,</span> <span class="n">order_id</span><span class="o">=</span><span class="s">"A123"</span><span class="p">,</span> <span class="n">amount</span><span class="o">=</span><span class="mf">40.00</span><span class="p">))</span>
</code></pre></div></div>

<p>The first call fails. The second one succeeds, once <code class="language-plaintext highlighter-rouge">get_customer</code> has actually run in between. No system prompt was involved in either outcome — the check gives the same result for any code calling these two functions in the wrong order, model or no model.</p>

<p>For comparison, the example project also keeps a prompt-only version of the same rule around:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">PROMPT_ONLY_SYSTEM</span> <span class="o">=</span> <span class="p">(</span>
    <span class="s">"You are a customer support agent. Always call get_customer before "</span>
    <span class="s">"calling process_refund. Never skip this step."</span>
<span class="p">)</span>
</code></pre></div></div>

<p>That sentence isn’t wrong, and it isn’t useless — a clear system prompt still helps the model reach for the right tool in the right order most of the time. It’s just not the thing actually doing the protecting. If this prompt were the only defense, <code class="language-plaintext highlighter-rouge">demo_prerequisite_gate()</code>’s direct call would have gone straight through to <code class="language-plaintext highlighter-rouge">process_refund</code> with nothing standing in front of it, because a prompt only has influence over a model — and that direct call never involved a model at all.</p>

<p>A plain <code class="language-plaintext highlighter-rouge">if</code> statement inside <code class="language-plaintext highlighter-rouge">process_refund</code> is one way to build a gate like this. It’s not the only one. The Agent SDK has a dedicated mechanism for the same job — hooks that sit between the model and a tool call and can block it before it ever runs, without the tool’s own code having to know anything about verification. A hand-written check and an SDK hook are both “programmatic enforcement” in the sense that matters here: code decides, not a sentence in a prompt. The difference is where that code lives and how it’s wired in, and that’s specific enough to deserve its own part — Part 5 covers Agent SDK hooks directly.</p>

<p>You can find the full file on <a href="https://github.com/distvan/claude-exam-objectives/blob/master/1.4%20Multi-step%20workflows/multistep_workflow_minimal.py">GitHub</a>. A <a href="https://github.com/distvan/claude-exam-objectives/blob/master/1.4%20Multi-step%20workflows/multistep_workflow_minimal.php">PHP port</a> is available too, built the same way as the earlier parts.</p>

<h2 id="one-message-more-than-one-problem">One message, more than one problem</h2>

<p>Real support messages rarely stick to one topic. “Check on order A123, and also refund order B456” is two separate concerns in one sentence, and a naive agent can end up half-answering one of them, or losing track of the second one while it’s busy explaining the first.</p>

<p>The example project doesn’t need any special machinery for this. The same agentic loop from Part 1 already handles it, because nothing in that loop forces tool calls to happen one at a time. A single response from the model can carry more than one <code class="language-plaintext highlighter-rouge">tool_use</code> block, and the loop already collects every one of them before it calls the model again:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">calls</span> <span class="o">=</span> <span class="p">[</span><span class="n">b</span> <span class="k">for</span> <span class="n">b</span> <span class="ow">in</span> <span class="n">response</span><span class="p">.</span><span class="n">content</span> <span class="k">if</span> <span class="n">b</span><span class="p">.</span><span class="nb">type</span> <span class="o">==</span> <span class="s">"tool_use"</span><span class="p">]</span>
<span class="k">if</span> <span class="nb">len</span><span class="p">(</span><span class="n">calls</span><span class="p">)</span> <span class="o">&gt;</span> <span class="mi">1</span><span class="p">:</span>
    <span class="k">print</span><span class="p">(</span><span class="sa">f</span><span class="s">"  [</span><span class="si">{</span><span class="n">label</span><span class="si">}</span><span class="s">] </span><span class="si">{</span><span class="nb">len</span><span class="p">(</span><span class="n">calls</span><span class="p">)</span><span class="si">}</span><span class="s"> tool calls in this turn (parallel)"</span><span class="p">)</span>
</code></pre></div></div>

<p>Given a two-order message, the agent verifies the customer once, then looks up both orders. They don’t depend on each other, so nothing stops them from landing in the same turn. Only after both results are back does it write one combined reply covering both concerns. The decomposition itself is the model’s job, not something the code has to spell out — a system prompt that expects multiple concerns and expects one final answer is enough, and the existing loop takes care of the rest.</p>

<h2 id="a-handoff-a-human-can-actually-use">A handoff a human can actually use</h2>

<p>Not every case ends with a tool result. Sometimes the agent hits a wall — an order that isn’t in the system, a request outside its authority — and the right move is to stop and hand the case to a person. What matters at that point is what the handoff actually contains.</p>

<p>A human picking up an escalated case usually doesn’t get the conversation transcript. They get whatever fields the escalation tool was called with. If those fields are vague — “customer has a problem, please help” — the human has to start the investigation from zero, which defeats the entire point of escalating a case the bot already partly worked through.</p>

<p><code class="language-plaintext highlighter-rouge">escalate_to_human</code> is written so that a vague handoff isn’t even possible, using the same tool schema every other tool in the project already uses — this time with required fields:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="s">"escalate_to_human"</span><span class="p">:</span> <span class="p">{</span>
    <span class="p">...</span>
    <span class="s">"input_schema"</span><span class="p">:</span> <span class="p">{</span>
        <span class="s">"type"</span><span class="p">:</span> <span class="s">"object"</span><span class="p">,</span>
        <span class="s">"properties"</span><span class="p">:</span> <span class="p">{</span>
            <span class="s">"customer_id"</span><span class="p">:</span> <span class="p">{</span><span class="s">"type"</span><span class="p">:</span> <span class="s">"string"</span><span class="p">},</span>
            <span class="s">"root_cause"</span><span class="p">:</span> <span class="p">{</span><span class="s">"type"</span><span class="p">:</span> <span class="s">"string"</span><span class="p">},</span>
            <span class="s">"recommended_action"</span><span class="p">:</span> <span class="p">{</span><span class="s">"type"</span><span class="p">:</span> <span class="s">"string"</span><span class="p">},</span>
            <span class="s">"refund_amount"</span><span class="p">:</span> <span class="p">{</span><span class="s">"type"</span><span class="p">:</span> <span class="s">"number"</span><span class="p">},</span>
        <span class="p">},</span>
        <span class="s">"required"</span><span class="p">:</span> <span class="p">[</span><span class="s">"customer_id"</span><span class="p">,</span> <span class="s">"root_cause"</span><span class="p">,</span> <span class="s">"recommended_action"</span><span class="p">],</span>
    <span class="p">},</span>
<span class="p">},</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">customer_id</code>, <code class="language-plaintext highlighter-rouge">root_cause</code>, and <code class="language-plaintext highlighter-rouge">recommended_action</code> are required — the model can’t call this tool without supplying them, because a tool call missing a required field isn’t a valid call in the first place. <code class="language-plaintext highlighter-rouge">refund_amount</code> stays optional, since plenty of escalations don’t have a dollar figure attached yet — the “order isn’t even in the system” case in the demo is exactly that. When the tool runs, it prints exactly what a human needs to start working: who the customer is, why this needs a person, and what the agent already thinks should happen next.</p>

<h2 id="critical-design-decisions">Critical design decisions</h2>

<ul>
  <li><strong>Enforcement lives in the tool, not the prompt</strong>: <code class="language-plaintext highlighter-rouge">process_refund</code> checks its own prerequisite before doing anything else. A system prompt can guide; only code sitting in the execution path can guarantee.</li>
  <li><strong>Test the gate without the model</strong>: calling a protected tool directly, the way <code class="language-plaintext highlighter-rouge">demo_prerequisite_gate()</code> does, is the fastest way to prove a gate holds no matter what any model decides to do.</li>
  <li><strong>Decomposition doesn’t need new code</strong>: parallel tool calls already exist in the loop from Part 1. A multi-concern message is handled by the same mechanism Part 3 used for parallel subagent spawning — just aimed at ordinary tools this time.</li>
  <li><strong>Required fields make a handoff complete by construction</strong>: putting <code class="language-plaintext highlighter-rouge">customer_id</code>, <code class="language-plaintext highlighter-rouge">root_cause</code>, and <code class="language-plaintext highlighter-rouge">recommended_action</code> in the schema’s <code class="language-plaintext highlighter-rouge">required</code> list means an incomplete handoff isn’t a call the model can make — not just a call it’s told not to make.</li>
  <li><strong>Optional fields for what isn’t always known yet</strong>: <code class="language-plaintext highlighter-rouge">refund_amount</code> stays optional because forcing a number into every escalation would push the model to guess one it doesn’t actually have.</li>
</ul>

<h2 id="consequences">Consequences</h2>

<ul>
  <li><strong>A prompt-only rule protecting a financial step</strong> -&gt; a non-zero fraction of conversations skip it, close to the 12% figure in the exam guide’s own sample question. The instruction isn’t wrong; it just isn’t a guarantee.</li>
  <li><strong>No gate at all</strong> -&gt; the failure only shows up in production logs, after the wrong account has already been refunded, instead of failing loudly the moment it happens.</li>
  <li><strong>Sequential-only concern handling</strong> -&gt; a two-part request pays for two round trips it didn’t need, the same latency cost Part 3 covered for subagents, just showing up here in an ordinary support conversation.</li>
  <li><strong>Optional or missing fields on an escalation tool</strong> -&gt; a human agent picks up a case with half the picture, and ends up re-doing the investigation the bot already did, which is the entire cost escalation was supposed to save.</li>
</ul>

<p>Enforcement and handoff aren’t really new ideas by themselves. They’re the same lesson from Part 1 — that a loop has to check something for certain instead of hoping the model got it right — applied to two new places: a gate between two tool calls, and a schema on the last one before a person takes over.</p>]]></content><author><name>Istvan Dobrentei</name></author><category term="AI" /><category term="Claude" /><summary type="html"><![CDATA[Why a prompt instruction alone can't guarantee a critical step happens in order - a programmatic prerequisite gate in process_refund, testing that gate without a model in the loop, handling multi-concern messages with parallel tool calls, and required fields that make an escalation to a human complete by construction.]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="https://www.en.dobrenteiistvan.hu/assets/images/articles/multi-step-workflows.svg" /><media:content medium="image" url="https://www.en.dobrenteiistvan.hu/assets/images/articles/multi-step-workflows.svg" xmlns:media="http://search.yahoo.com/mrss/" /></entry><entry><title type="html">Production Agent Architecture: Configure Subagent Invocation (Part 3)</title><link href="https://www.en.dobrenteiistvan.hu/blog/configure-subagent-invocation.html" rel="alternate" type="text/html" title="Production Agent Architecture: Configure Subagent Invocation (Part 3)" /><published>2026-08-02T00:00:00+00:00</published><updated>2026-08-02T00:00:00+00:00</updated><id>https://www.en.dobrenteiistvan.hu/blog/configure-subagent-invocation</id><content type="html" xml:base="https://www.en.dobrenteiistvan.hu/blog/configure-subagent-invocation.html"><![CDATA[<p><em>Lessons Learned While Preparing for the Claude Architect Certification</em></p>

<h2 id="a-different-example-on-purpose">A different example, on purpose</h2>

<p>Part 2 stayed with one example the whole time: a customer support agent, split into order-status, pricing, and returns subagents. This part switches to a different one — a small research pipeline, with a coordinator that spawns a web-search subagent, a document-analysis subagent, and a synthesis subagent.</p>

<p>That’s not a random change. The official objective for this part is written in exactly those terms: passing web search results and document analysis outputs into a synthesis subagent’s prompt. It’s closer to a research pipeline than to a support ticket, so the example follows the objective instead of forcing the old one to fit.</p>

<p>What doesn’t change is the underlying question. Part 2 answered <em>why</em> you’d split into a coordinator and subagents, and what design decisions that split creates. This part answers something narrower and more mechanical: once you’ve decided to spawn a subagent, what exactly do you configure, and what do you have to hand it explicitly, for that spawn to actually work?</p>

<h2 id="the-task-tool-and-agentdefinition">The Task tool and AgentDefinition</h2>

<p>A coordinator spawns a subagent through the <code class="language-plaintext highlighter-rouge">Task</code> tool. For that to work at all, <code class="language-plaintext highlighter-rouge">Task</code> has to be in the coordinator’s own list of allowed tools — without it, the coordinator can still reason about delegating, it just has no way to actually do it. Nothing crashes. The coordinator simply never spawns anyone, and from the outside that can look like a model that “doesn’t want” to delegate, when the real problem is one missing entry in its own tool list.</p>

<p>Each subagent it spawns is described by an <code class="language-plaintext highlighter-rouge">AgentDefinition</code>: a name, a description, a system prompt, and the tools it’s allowed to use. Part 2 already had all four of these — a system prompt, a tool list, and tool implementations passed straight into <code class="language-plaintext highlighter-rouge">run_agentic_loop()</code>, one hardcoded per subagent function. <code class="language-plaintext highlighter-rouge">AgentDefinition</code> just bundles what was scattered across separate function arguments into one config object per subagent.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="o">@</span><span class="n">dataclass</span>
<span class="k">class</span> <span class="nc">AgentDefinition</span><span class="p">:</span>
    <span class="n">name</span><span class="p">:</span> <span class="nb">str</span>
    <span class="n">description</span><span class="p">:</span> <span class="nb">str</span>
    <span class="n">system_prompt</span><span class="p">:</span> <span class="nb">str</span>
    <span class="n">tools</span><span class="p">:</span> <span class="nb">list</span><span class="p">[</span><span class="nb">dict</span><span class="p">[</span><span class="nb">str</span><span class="p">,</span> <span class="n">Any</span><span class="p">]]</span>
    <span class="n">tool_impls</span><span class="p">:</span> <span class="nb">dict</span><span class="p">[</span><span class="nb">str</span><span class="p">,</span> <span class="n">Callable</span><span class="p">[...,</span> <span class="nb">str</span><span class="p">]]</span>
    <span class="n">max_turns</span><span class="p">:</span> <span class="nb">int</span> <span class="o">=</span> <span class="n">DEFAULT_MAX_ITERATIONS</span>


<span class="k">def</span> <span class="nf">spawn_subagent</span><span class="p">(</span><span class="n">agent</span><span class="p">:</span> <span class="n">AgentDefinition</span><span class="p">,</span> <span class="n">task_prompt</span><span class="p">:</span> <span class="nb">str</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">str</span><span class="p">:</span>
    <span class="k">return</span> <span class="n">run_agentic_loop</span><span class="p">(</span>
        <span class="n">user_message</span><span class="o">=</span><span class="n">task_prompt</span><span class="p">,</span>
        <span class="n">system</span><span class="o">=</span><span class="n">agent</span><span class="p">.</span><span class="n">system_prompt</span><span class="p">,</span>
        <span class="n">tools</span><span class="o">=</span><span class="n">agent</span><span class="p">.</span><span class="n">tools</span><span class="p">,</span>
        <span class="n">tool_impls</span><span class="o">=</span><span class="n">agent</span><span class="p">.</span><span class="n">tool_impls</span><span class="p">,</span>
        <span class="n">label</span><span class="o">=</span><span class="n">agent</span><span class="p">.</span><span class="n">name</span><span class="p">,</span>
        <span class="n">max_iterations</span><span class="o">=</span><span class="n">agent</span><span class="p">.</span><span class="n">max_turns</span><span class="p">,</span>
    <span class="p">)</span>
</code></pre></div></div>

<p>The example project’s coordinator stands in for <code class="language-plaintext highlighter-rouge">Task</code> with three named tools instead of one generic one — <code class="language-plaintext highlighter-rouge">consult_web_search</code>, <code class="language-plaintext highlighter-rouge">consult_document_analysis</code>, <code class="language-plaintext highlighter-rouge">consult_synthesis</code> — so the demo stays runnable without a real SDK dependency. The idea is the same either way: one config object per subagent, one function that spawns it. You can find the full file on <a href="https://github.com/distvan/claude-exam-objectives/blob/master/1.3%20Configure%20subagent%20invocation/subagent_invocation_minimal.py">GitHub</a>.</p>

<p>A <a href="https://github.com/distvan/claude-exam-objectives/blob/master/1.3%20Configure%20subagent%20invocation/subagent_invocation_minimal.php">PHP port</a> is available too, built on Anthropic’s official PHP SDK. <code class="language-plaintext highlighter-rouge">AgentDefinition</code> becomes a small readonly class instead of a dataclass, but it holds the exact same four things: name, description, system prompt, and tool restrictions.</p>

<h2 id="context-is-never-inherited--you-pass-it-explicitly">Context is never inherited — you pass it, explicitly</h2>

<p>This is the part that actually trips people up: a subagent does not automatically see the coordinator’s conversation. It doesn’t remember being spawned last time, either. Every <code class="language-plaintext highlighter-rouge">task_prompt</code> you hand <code class="language-plaintext highlighter-rouge">spawn_subagent()</code> is the <em>entire</em> world that subagent knows about. If the coordinator already found two sources and read both of them, and it wants a synthesis subagent to write about them, it has to put what it found directly into that subagent’s prompt. Nothing carries over on its own.</p>

<p>The document-analysis subagent in the example project is written to make this easy to do right. Its system prompt asks for one fixed line of output: <code class="language-plaintext highlighter-rouge">CLAIM: &lt;the claim&gt; | SOURCE: &lt;url&gt; | PAGE: &lt;page number&gt;</code>. That’s a structured format, not free prose — it separates the actual claim from the metadata that lets someone check it later. When the coordinator calls <code class="language-plaintext highlighter-rouge">consult_synthesis</code>, it passes those lines through as-is:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="s">"consult_synthesis"</span><span class="p">:</span> <span class="k">lambda</span> <span class="n">question</span><span class="p">,</span> <span class="n">findings</span><span class="p">:</span> <span class="n">spawn_subagent</span><span class="p">(</span>
    <span class="n">SYNTHESIS_AGENT</span><span class="p">,</span> <span class="sa">f</span><span class="s">"Research question: </span><span class="si">{</span><span class="n">question</span><span class="si">}</span><span class="se">\n\n</span><span class="s">Findings:</span><span class="se">\n</span><span class="si">{</span><span class="n">findings</span><span class="si">}</span><span class="s">"</span>
<span class="p">),</span>
</code></pre></div></div>

<p>The synthesis subagent has no tools at all. Whatever arrives in that <code class="language-plaintext highlighter-rouge">findings</code> string is everything it has to work with — which is exactly the point. There’s nowhere else for the information to come from.</p>

<h2 id="parallel-spawning-multiple-task-calls-one-response">Parallel spawning: multiple Task calls, one response</h2>

<p>Nothing about the agentic loop forces subagents to run one at a time. A single coordinator response can contain more than one <code class="language-plaintext highlighter-rouge">tool_use</code> block, and the loop from Part 1 already collects every one of them before calling the model again. So if the coordinator wants to read two sources, it can ask for both <code class="language-plaintext highlighter-rouge">consult_document_analysis</code> calls in the same turn instead of spacing them across two separate turns:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">calls</span> <span class="o">=</span> <span class="p">[</span><span class="n">b</span> <span class="k">for</span> <span class="n">b</span> <span class="ow">in</span> <span class="n">response</span><span class="p">.</span><span class="n">content</span> <span class="k">if</span> <span class="n">b</span><span class="p">.</span><span class="nb">type</span> <span class="o">==</span> <span class="s">"tool_use"</span><span class="p">]</span>
<span class="k">if</span> <span class="nb">len</span><span class="p">(</span><span class="n">calls</span><span class="p">)</span> <span class="o">&gt;</span> <span class="mi">1</span><span class="p">:</span>
    <span class="k">print</span><span class="p">(</span><span class="sa">f</span><span class="s">"  [</span><span class="si">{</span><span class="n">label</span><span class="si">}</span><span class="s">] </span><span class="si">{</span><span class="nb">len</span><span class="p">(</span><span class="n">calls</span><span class="p">)</span><span class="si">}</span><span class="s"> subagents spawned in this turn (parallel)"</span><span class="p">)</span>
</code></pre></div></div>

<p>This isn’t new machinery. It’s the same mechanism Part 2 used for the coordinator delegating to more than one subagent per question. What’s different here is the reason: two independent document reads don’t depend on each other, so there’s no reason to make one wait for the other to finish before it even starts.</p>

<h2 id="coordinator-prompts-goals-not-steps">Coordinator prompts: goals, not steps</h2>

<p>The example project’s coordinator system prompt doesn’t say “call web-search, then call document-analysis on each result, then call synthesis.” It says the final report has to cite at least two independent sources and flag any disagreement between them, and leaves the actual sequence up to the model.</p>

<p>A procedural version would look like this instead:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">PROCEDURAL_COORDINATOR_SYSTEM</span> <span class="o">=</span> <span class="p">(</span>
    <span class="s">"You are a research coordinator. Step 1: call consult_web_search once. "</span>
    <span class="s">"Step 2: call consult_document_analysis on every URL from step 1. "</span>
    <span class="s">"Step 3: call consult_synthesis with the combined findings. "</span>
    <span class="s">"Always follow these steps in this exact order."</span>
<span class="p">)</span>
</code></pre></div></div>

<p>That version works fine as long as the research goes exactly as planned. The moment one step doesn’t — a source turns out to be unreadable, or one search isn’t enough to answer the question — a fixed procedure has nothing to fall back on. It was told steps, not what a good outcome looks like, so it can’t adapt when the steps stop making sense. A goal-and-criteria prompt can still decide to search again, skip a broken source, or ask for a third document, because it was told what “done” means instead of what to type next.</p>

<h2 id="fresh-spawn-vs-forked-session">Fresh spawn vs. forked session</h2>

<p>Every <code class="language-plaintext highlighter-rouge">spawn_subagent()</code> call in this example starts a subagent with nothing behind it — no history, no shared state, a blank slate that only knows what’s in its prompt. That’s the right default when a subagent’s job is genuinely independent, like reading one source.</p>

<p>Sometimes it isn’t what you want. If two subagents both need to build on the same analysis so far, and then try different approaches from that shared point, starting each one from zero means re-explaining everything they already have in common. Forking a session solves that differently: instead of a fresh subagent, you branch an existing one, so both copies start from the same baseline and only diverge from there. Task 1.3 only asks you to know this option exists as the alternative to a fresh spawn; managing it in practice — resuming, forking, deciding when prior context has gone stale — is its own task statement.</p>

<h2 id="critical-design-decisions">Critical design decisions</h2>

<ul>
  <li><strong>Task in allowedTools</strong>: this is a prerequisite, not a preference. A coordinator without it can’t spawn anything, no matter how well everything else is configured.</li>
  <li><strong>What to pass, explicitly</strong>: the complete findings from prior subagents, not a summary of them. A coordinator that paraphrases before handing off is already losing information before the next subagent even starts.</li>
  <li><strong>Structured over free-text handoffs</strong>: a fixed format (claim, source, page) survives being passed from one subagent’s output into another subagent’s input. Prose doesn’t — it invites paraphrasing at every step it passes through.</li>
  <li><strong>Parallel emission</strong>: independent subagent calls belong in the same turn. Spacing them across separate turns adds latency for no benefit, the same way an unnecessary sequential dependency would.</li>
  <li><strong>Goals over steps in the coordinator’s prompt</strong>: a fixed procedure only works when nothing unexpected happens. A prompt that states the quality bar, not the sequence, is what lets the coordinator adapt when it doesn’t.</li>
  <li><strong>Fresh vs. forked</strong>: spawn fresh when a subagent’s task is independent of anything else in flight; fork when it needs to continue from work already done and try a variant.</li>
</ul>

<h2 id="consequences">Consequences</h2>

<ul>
  <li><strong>No <code class="language-plaintext highlighter-rouge">Task</code> in allowedTools</strong> -&gt; the coordinator can reason its way toward delegating and never actually manage it. Nothing errors. It just quietly never spawns a subagent, and that’s easy to mistake for a prompting problem instead of a missing permission.</li>
  <li><strong>Assumed context inheritance</strong> -&gt; a subagent spawned with a short instruction and none of the coordinator’s findings either answers off information it doesn’t have, or asks a question the coordinator already knew the answer to. Either way, the coordinator paid for a subagent call that couldn’t succeed.</li>
  <li><strong>Unstructured handoffs</strong> -&gt; the example project’s lossy-handoff demo shows this directly: the same synthesis subagent, given the same findings but paraphrased into plain sentences first, has nothing left to cite. The claim survives. The attribution doesn’t.</li>
  <li><strong>Sequential-only spawning</strong> -&gt; independent subagent calls wait on each other for no reason, adding latency a single coordinator turn could have avoided.</li>
  <li><strong>A procedural coordinator prompt</strong> -&gt; the coordinator executes its steps even when the situation no longer matches them — a missing source, an unanswerable sub-question — because it was never given a goal to fall back on, only a sequence to finish.</li>
</ul>

<p>None of this replaces what Part 2 already covers. A subagent still needs a reason to exist and a clear slice of the domain. This part is what makes a subagent’s <em>invocation</em> actually correct: the tool that spawns it, the context it’s handed, and the prompt that tells it — and the coordinator — what it’s actually there to do.</p>]]></content><author><name>Istvan Dobrentei</name></author><category term="AI" /><category term="Claude" /><summary type="html"><![CDATA[What actually has to be configured for a subagent spawn to work in production - the Task tool and AgentDefinition, why context is never inherited and must be passed explicitly, structured handoffs, parallel vs. sequential spawning, goal-based coordinator prompts, and fresh spawns vs. forked sessions.]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="https://www.en.dobrenteiistvan.hu/assets/images/articles/configure-subagent-invocation.svg" /><media:content medium="image" url="https://www.en.dobrenteiistvan.hu/assets/images/articles/configure-subagent-invocation.svg" xmlns:media="http://search.yahoo.com/mrss/" /></entry><entry><title type="html">Production Agent Architecture: The Agentic Loop (Part 1)</title><link href="https://www.en.dobrenteiistvan.hu/blog/agentic-loop.html" rel="alternate" type="text/html" title="Production Agent Architecture: The Agentic Loop (Part 1)" /><published>2026-08-01T00:00:00+00:00</published><updated>2026-08-01T00:00:00+00:00</updated><id>https://www.en.dobrenteiistvan.hu/blog/agentic-loop</id><content type="html" xml:base="https://www.en.dobrenteiistvan.hu/blog/agentic-loop.html"><![CDATA[<p><em>Lessons learned while preparing for the Claude Architect Certification</em></p>

<h2 id="what-is-an-agentic-loop">What is an agentic loop?</h2>

<p>When people hear the word “agent,” they often imagine an AI that can think and solve problems on its own. But many apps that call themselves “AI agents” are really just one prompt and one response. Let’s look at an example.</p>

<p>Suppose we want to build a customer support assistant. It should answer questions about orders.</p>

<p>A user asks:</p>

<blockquote>
  <p>“My package hasn’t arrived yet. Can you check what’s happening?”</p>
</blockquote>

<p>Our first version is simple. The app sends the user’s message to the model, together with a system prompt like this:</p>

<blockquote>
  <p>You are a helpful customer support assistant. Answer the user’s questions.</p>
</blockquote>

<p>The model replies:</p>

<blockquote>
  <p>“I’m sorry your package hasn’t arrived. Please contact our support team.”</p>
</blockquote>

<p>Technically, this works. The model wrote a fluent, polite answer. The user got a reply. But did it actually solve the problem? Not really.</p>

<p>The model has no way to check the order status. It cannot contact the shipping company. It cannot find out if the package was delivered, delayed, or lost.</p>

<p>It cannot even ask the user for the missing order number. Most importantly, it cannot decide what information it needs before it answers. It just writes text from what it already knows.</p>

<p>This is the limit of a single model call. The model can only reason over the context you give it, no matter how capable it gets. If it needs more information, or needs to talk to another system, it gets stuck.</p>

<p>This is exactly where an agentic loop becomes useful.</p>

<p>Instead of producing one response, the app lets the model work in repeated steps. On each step, the model can:</p>

<ul>
  <li>look at the current situation</li>
  <li>decide what information is missing</li>
  <li>call a tool to get that information</li>
  <li>check the result</li>
  <li>and continue until the task is done</li>
</ul>

<p>We call this repeating cycle an <strong>agentic loop</strong>.</p>

<p>The loop doesn’t make the model smarter. It gives the app a clear, structured process. The model gathers information, makes decisions, and reacts to new information, one step at a time, until the task is complete.</p>

<h2 id="a-pseudocode-example">A pseudocode example</h2>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>messages = [user_message]

loop:
    response = call_model(messages, tools)

    if response.stop_reason == "end_turn":
        return response.text

    if response.stop_reason == "tool_use":
        result = run_tool(response.tool_call)

        messages.append({"role": "assistant", "content": response.content})
        messages.append({"role": "user", "content": [tool_result(response.tool_call.id, result)]})
        continue

    raise UnhandledStopReason(response.stop_reason)
</code></pre></div></div>

<p>This pseudocode is close to the real thing. It’s not a simplified version made just for the article.</p>

<p>Here is <code class="language-plaintext highlighter-rouge">run_agentic_loop()</code> from the example project. It uses one tool, <code class="language-plaintext highlighter-rouge">get_weather</code>, instead of order lookups. The logic stays the same:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">run_agentic_loop</span><span class="p">(</span><span class="n">user_message</span><span class="p">:</span> <span class="nb">str</span><span class="p">,</span> <span class="n">max_iterations</span><span class="p">:</span> <span class="nb">int</span> <span class="o">=</span> <span class="n">DEFAULT_MAX_ITERATIONS</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">str</span><span class="p">:</span>
    <span class="n">messages</span><span class="p">:</span> <span class="nb">list</span><span class="p">[</span><span class="nb">dict</span><span class="p">[</span><span class="nb">str</span><span class="p">,</span> <span class="n">Any</span><span class="p">]]</span> <span class="o">=</span> <span class="p">[{</span><span class="s">"role"</span><span class="p">:</span> <span class="s">"user"</span><span class="p">,</span> <span class="s">"content"</span><span class="p">:</span> <span class="n">user_message</span><span class="p">}]</span>

    <span class="k">for</span> <span class="n">_</span> <span class="ow">in</span> <span class="nb">range</span><span class="p">(</span><span class="n">max_iterations</span><span class="p">):</span>
        <span class="n">response</span> <span class="o">=</span> <span class="n">call_model</span><span class="p">(</span><span class="n">messages</span><span class="p">)</span>

        <span class="k">if</span> <span class="n">response</span><span class="p">.</span><span class="n">stop_reason</span> <span class="o">==</span> <span class="s">"end_turn"</span><span class="p">:</span>
            <span class="k">return</span> <span class="n">extract_final_text</span><span class="p">(</span><span class="n">response</span><span class="p">)</span>

        <span class="k">if</span> <span class="n">response</span><span class="p">.</span><span class="n">stop_reason</span> <span class="o">!=</span> <span class="s">"tool_use"</span><span class="p">:</span>
            <span class="k">raise</span> <span class="nb">RuntimeError</span><span class="p">(</span><span class="sa">f</span><span class="s">"Unhandled stop_reason: </span><span class="si">{</span><span class="n">response</span><span class="p">.</span><span class="n">stop_reason</span><span class="si">}</span><span class="s">"</span><span class="p">)</span>

        <span class="n">tool_results</span> <span class="o">=</span> <span class="n">run_requested_tools</span><span class="p">(</span><span class="n">response</span><span class="p">)</span>
        <span class="n">append_tool_turn</span><span class="p">(</span><span class="n">messages</span><span class="p">,</span> <span class="n">response</span><span class="p">,</span> <span class="n">tool_results</span><span class="p">)</span>

    <span class="k">raise</span> <span class="nb">RuntimeError</span><span class="p">(</span><span class="sa">f</span><span class="s">"Exceeded max_iterations (</span><span class="si">{</span><span class="n">max_iterations</span><span class="si">}</span><span class="s">) without end_turn"</span><span class="p">)</span>
</code></pre></div></div>

<p>These four branches always run in the same order. <code class="language-plaintext highlighter-rouge">stop_reason</code> decides what happens next — nothing else does.</p>

<p><code class="language-plaintext highlighter-rouge">call_model</code>, <code class="language-plaintext highlighter-rouge">run_requested_tools</code>, and <code class="language-plaintext highlighter-rouge">append_tool_turn</code> each do one small job. You can find the full file on <a href="https://github.com/distvan/claude-exam-objectives/blob/master/1.1%20Agentic-loops/agentic_loop_minimal.py">GitHub</a>.</p>

<p>The same loop, tool, and demos are also available as a <a href="https://github.com/distvan/claude-exam-objectives/blob/master/1.1%20Agentic-loops/agentic_loop_minimal.php">PHP port</a>, using Anthropic’s official PHP SDK (<code class="language-plaintext highlighter-rouge">anthropic-ai/sdk</code>). The <code class="language-plaintext highlighter-rouge">tool_use</code>/<code class="language-plaintext highlighter-rouge">tool_result</code> round trip is identical — this part of an agentic loop isn’t a Python idiom, it’s how the API itself works, regardless of which language calls it.</p>

<p>One real difference showed up only once this ran against the live API: the PHP SDK exposes the response’s <code class="language-plaintext highlighter-rouge">stop_reason</code> field as <code class="language-plaintext highlighter-rouge">$response-&gt;stopReason</code> — camelCase, not the snake_case you’d guess from the Python SDK or the API docs. Small detail, but it’s exactly the kind of thing that only surfaces by actually running the code, not by reading it.</p>

<h2 id="model-driven-decisions-not-a-fixed-pipeline">Model-driven decisions, not a fixed pipeline</h2>

<p>There’s one more distinction worth making explicit: nothing in this loop forces the model to call a tool. On every single call, the model looks at the conversation and decides for itself whether it needs <code class="language-plaintext highlighter-rouge">get_weather</code> at all.</p>

<p>Ask the loop “What’s the weather in Budapest?” and it calls the tool, because it needs to. Ask it “What’s 2 + 2?” and it answers directly — <code class="language-plaintext highlighter-rouge">stop_reason</code> comes back <code class="language-plaintext highlighter-rouge">end_turn</code> on the very first call, with no <code class="language-plaintext highlighter-rouge">tool_use</code> block at all. Same loop, same tool available, different question, different path through the code.</p>

<p>Compare that with a hardcoded pipeline: a fixed sequence like “always call <code class="language-plaintext highlighter-rouge">get_weather</code>, then always format the result.” That kind of pipeline doesn’t reason about whether the step is needed — it just runs it, every time, regardless of what was actually asked. The example project’s <a href="https://github.com/distvan/claude-exam-objectives/blob/master/1.1%20Agentic-loops/agentic_loop_minimal.py"><code class="language-plaintext highlighter-rouge">run_hardcoded_pipeline()</code></a> does exactly that, so you can see the difference side by side.</p>

<p>This is what “model-driven decision-making” means in practice. The loop’s code doesn’t encode a sequence of steps. It encodes a rule: call the model, see what it wants, do that, repeat. What actually happens on any given run is up to the model, not the control flow.</p>

<h2 id="critical-design-decisions">Critical design decisions</h2>

<p>Building the loop itself is usually not the hard part. In a real project, nobody asks an architect “what is an agentic loop?” They ask for a loop that works reliably in production.</p>

<p>The hard part is different. You need to decide when the loop should keep going, when it should stop, and how it should recover when something breaks. At what point should the agent stop acting on its own and hand the task to a human?</p>

<p>A well-designed agentic system needs clear rules for:</p>

<ul>
  <li><strong>Termination</strong>: How does the agent know it’s done? This is the point the exam checks most closely. <code class="language-plaintext highlighter-rouge">stop_reason</code> is the only signal you should trust — not the text content, not a guess. You can still use <code class="language-plaintext highlighter-rouge">max_iterations</code> as a safety net, but only as a last resort. It should never be the normal way to decide a conversation is over.</li>
  <li><strong>Retries</strong>: Which failures are worth trying again?</li>
  <li><strong>Error handling</strong>: How does the workflow recover safely?</li>
  <li><strong>State management</strong>: What information should stay between steps, and what should get dropped?</li>
  <li><strong>Context management</strong>: What information does the model actually need to carry forward?</li>
  <li><strong>Human in the loop</strong>: When should a person take over?</li>
</ul>

<h2 id="consequences">Consequences</h2>

<p>Skipping these decisions doesn’t make the loop simpler. It just moves the problem somewhere less visible.</p>

<ul>
  <li><strong>No termination rule</strong> -&gt; the loop stops too early, or never stops at all.</li>
  <li><strong>No retry strategy</strong> -&gt; one small, temporary failure (a network blip, a rate limit) kills the whole task. A second try might have worked.</li>
  <li><strong>No state or context management</strong> -&gt; the message history keeps growing until you hit the context limit. Or the model loses track of the original goal.</li>
  <li><strong>No human-in-the-loop rule</strong> -&gt; the agent keeps acting on its own, even in cases where a person should sign off first, like a payment or another action you can’t undo.</li>
</ul>

<p>This is the real gap between a demo that works on a good day and a system an architect is willing to stand behind. None of these ideas are complicated. You just have to decide them on purpose, instead of by accident.</p>]]></content><author><name>Istvan Dobrentei</name></author><category term="AI" /><category term="Claude" /><summary type="html"><![CDATA[Why a single model call isn't a real AI agent - what an agentic loop actually is, a working Python example, and the design decisions around termination, retries, error handling, and human-in-the-loop that decide if an agent survives production.]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="https://www.en.dobrenteiistvan.hu/assets/images/articles/agentic-loop.svg" /><media:content medium="image" url="https://www.en.dobrenteiistvan.hu/assets/images/articles/agentic-loop.svg" xmlns:media="http://search.yahoo.com/mrss/" /></entry><entry><title type="html">Production Agent Architecture: The Coordinator Subagent (Part 2)</title><link href="https://www.en.dobrenteiistvan.hu/blog/coordinator-subagent.html" rel="alternate" type="text/html" title="Production Agent Architecture: The Coordinator Subagent (Part 2)" /><published>2026-08-01T00:00:00+00:00</published><updated>2026-08-01T00:00:00+00:00</updated><id>https://www.en.dobrenteiistvan.hu/blog/coordinator-subagent</id><content type="html" xml:base="https://www.en.dobrenteiistvan.hu/blog/coordinator-subagent.html"><![CDATA[<p><em>Lessons Learned While Preparing for the Claude Architect Certification</em></p>

<h2 id="from-one-agent-to-many">From one agent to many</h2>

<p>In Part 1, a single-tool agent looked up the weather for a city. Adding a second tool there would have taken almost no effort — just one more entry in the tools list. The loop already knows how to call any tool and use the result. Nothing about the control flow has to change.</p>

<p>This part switches the example to customer support, because Task 1.2’s decomposition problem doesn’t really show up until more than one plausible tool sits in the same list. Say the product team wants an agent that can look up an order’s status (<code class="language-plaintext highlighter-rouge">get_order_status</code>), and now also wants it to tell customers whether they could still get a better price on what they bought. That means a new tool, <code class="language-plaintext highlighter-rouge">get_price_history</code>. It also means a new policy to explain, plus new edge cases. For example: what if the order is too old for price data to exist?</p>

<p>Nothing stops you from adding this as a third tool on the same agent. But a few problems start to show up:</p>

<ul>
  <li>The system prompt now covers three topics at once: shipping, returns, and pricing. The model has to keep all of this in mind on every turn, even for a question about just one topic.</li>
  <li>Tool descriptions from different topics sit in the same list and start to compete for attention. This can make the model’s tool choice less accurate.</li>
  <li>A bug in the pricing logic can now break a conversation that was only about shipping. Everything shares the same context and the same loop.</li>
  <li>The problem doesn’t stop at three tools. Every new feature adds the same kind of strain again, and worse.</li>
</ul>

<p>A bigger prompt doesn’t fix this. The real fix is structural. You split the single agent into a <strong>coordinator</strong> and a set of <strong>subagents</strong>. The coordinator only delegates work and combines the answers. Each subagent is its own agentic loop from Part 1, focused on one topic, with its own tools and its own separate context.</p>

<p>This is the coordinator-subagent pattern. It isn’t a new kind of loop. It’s the same loop from Part 1, used one level deeper.</p>

<h2 id="a-pseudocode-example">A pseudocode example</h2>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>coordinator_loop(user_message):
    messages = [user_message]

    loop:
        response = call_model(messages, tools=SUBAGENT_TOOLS)

        if response.stop_reason == "end_turn":
            return response.text

        if response.stop_reason == "tool_use":
            # each "tool" here really means: delegate to a subagent —
            # a single response can ask for more than one subagent at once
            tool_results = []
            for tool_call in response.tool_calls:
                result = run_agentic_loop(tool_call.subtask)
                tool_results.append(tool_result(tool_call.id, result))

            messages.append({"role": "assistant", "content": response.content})
            messages.append({"role": "user", "content": tool_results})
            continue

        raise UnhandledStopReason(response.stop_reason)
</code></pre></div></div>

<p>This looks a lot like Part 1’s pseudocode. The only real change is what a “tool” does.</p>

<p>In Part 1, a tool call reached into a fake backend and returned a short string. Here, each tool call reaches into <code class="language-plaintext highlighter-rouge">run_agentic_loop()</code> — the same function from Part 1. It returns whatever that inner loop produces once it reaches its own <code class="language-plaintext highlighter-rouge">end_turn</code>.</p>

<p>The coordinator doesn’t know, and doesn’t need to know, that its “tool” is actually a full conversation with the model. From the coordinator’s side, it’s just a function: send a question, get an answer back.</p>

<p>One more detail is worth pointing out. The loop collects every <code class="language-plaintext highlighter-rouge">tool_use</code> block from a single response before it calls the model again. So the coordinator can ask for several subagents in the same turn. What you do with that — run them one after another, or run them at the same time — is exactly the “parallel vs. sequential” question below.</p>

<p>The example project (<a href="https://github.com/distvan/claude-exam-objectives/blob/master/1.2%20Coordinator-subagents/coordinator_subagent_minimal.py"><code class="language-plaintext highlighter-rouge">coordinator_subagent_minimal.py</code></a>) shows this in real code. <code class="language-plaintext highlighter-rouge">run_agentic_loop()</code> is written once. The coordinator, the order subagent, and the pricing subagent all call through it. Only the system prompt, the tools, and the tool implementations change between roles.</p>

<p>There’s also a <a href="https://github.com/distvan/claude-exam-objectives/blob/master/1.2%20Coordinator-subagents/coordinator_subagent_minimal.php">PHP port</a> of the same file, same three subagents, same narrow-vs-refining coordinator demo. Nothing about the coordinator-subagent pattern changes with the language — it’s still one <code class="language-plaintext highlighter-rouge">run_agentic_loop()</code>, called once per role, through Anthropic’s official PHP SDK instead of the Python one.</p>

<h2 id="critical-design-decisions">Critical design decisions</h2>

<p>In Part 1, the hard question was: when is a single loop done? In Part 2, the hard question is different: how many loops do you need, and how do they talk to each other?</p>

<ul>
  <li><strong>Task decomposition</strong>: How does the coordinator decide what to delegate, and to whom? In the example project, this is entirely up to the model’s judgment, based on the tool descriptions (<code class="language-plaintext highlighter-rouge">consult_order_subagent</code> vs. <code class="language-plaintext highlighter-rouge">consult_pricing_subagent</code>). In a real system, a vague tool description can easily make the coordinator delegate too much or too little — or decompose the topic too narrowly to begin with. More on that below.</li>
  <li><strong>Context isolation</strong>: What does each subagent actually see? This is the key point the code shows: every subagent starts with a fresh, narrow message history. It only gets the question it was given, not the coordinator’s full conversation. That’s why the pricing subagent never sees order data it doesn’t need: smaller context, narrower tool access, less room for mistakes.</li>
  <li><strong>Communication protocol</strong>: What shape does a delegation and its answer take? The example project keeps it simple: a plain text question in, a plain text answer out. Bigger systems often need structured data instead. Then the coordinator can react to a partial failure directly, instead of reading it out of a sentence.</li>
  <li><strong>Parallel vs. sequential execution</strong>: Can subagents run at the same time? A single response can hold more than one <code class="language-plaintext highlighter-rouge">tool_use</code> block, so independent subagents could in principle start together, before either one finishes. But if one subagent needs another one’s result first, they still have to run in order.</li>
  <li><strong>Error propagation</strong>: What happens if a subagent fails? It might run out of steps, hit a tool error, or get a question it can’t answer. Does the coordinator try again with a different question? Continue with a partial answer? Tell the user something went wrong? If you don’t decide this in advance, the coordinator can end up giving a confident answer built on missing information.</li>
  <li><strong>Aggregation and synthesis</strong>: How does the coordinator turn several subagent answers into one clear response? This takes real reasoning, not just formatting. If two subagents give answers that don’t fully agree, something has to sort that out — in this case, the coordinator’s own model call.</li>
  <li><strong>Depth and cost control</strong>: What stops a subagent from becoming a coordinator itself, and starting its own subagents again and again? By default, nothing does. You need a clear depth limit, a rule that says “this subagent can’t delegate further,” and a cost or token budget per branch. These are decisions the architect has to make on purpose.</li>
</ul>

<h2 id="when-two-subagents-arent-enough">When two subagents aren’t enough</h2>

<p>The order and pricing example only has two subagents, and with two subagents decomposition can’t really go wrong — there’s nowhere else to send a question. The moment you add a third, a different kind of problem shows up. Not a bug in the code. A gap in the decomposition itself.</p>

<p>The example project adds that third subagent: returns. Same shape as the other two — its own tool, its own isolated context, its own narrow job.</p>

<p>Now ask the coordinator a question that touches all three topics: <em>“Where’s my order A1001, is it still a good price, and can I return it if I don’t like it?”</em></p>

<p>If the coordinator’s system prompt and tool list were only ever built around order and pricing, the third part of that question just disappears. Nothing crashes. Nothing errors out. The coordinator confidently answers two-thirds of the question and never mentions the part it had no subagent for. From the user’s side, the missing return policy doesn’t look like a failure — it looks like it was never asked about.</p>

<p>That’s the real risk of narrow decomposition: not that the coordinator makes a wrong call, but that its own tool list quietly becomes a ceiling on what it can ever cover. <a href="https://github.com/distvan/claude-exam-objectives/blob/master/1.2%20Coordinator-subagents/coordinator_subagent_minimal.py"><code class="language-plaintext highlighter-rouge">run_narrow_coordinator()</code></a> reproduces exactly this. Give it the three-topic question above and watch the third topic vanish from the answer.</p>

<p>The fix isn’t to give the coordinator every subagent “just in case.” That just means synthesizing three answers for a question that only needed one. The real fix is two things done well: build a subagent set that actually spans the domain, and let the coordinator choose <em>dynamically</em> which of those subagents a given question needs — not a fixed subset, and not all of them by default.</p>

<p><a href="https://github.com/distvan/claude-exam-objectives/blob/master/1.2%20Coordinator-subagents/coordinator_subagent_minimal.py"><code class="language-plaintext highlighter-rouge">run_refining_coordinator()</code></a> shows both halves of that. It has all three subagents available, but its system prompt tells it to call only the ones a question actually touches. It also tells the model to check its own draft answer against the original question before finishing: if a part is still unaddressed, delegate again instead of stopping.</p>

<p>That second instruction is what an <strong>iterative refinement loop</strong> really is. It isn’t a new mechanism bolted onto the architecture — it’s the same loop from Part 1, given a reason to use more than one round. The coordinator was always allowed to call a subagent, look at the result, and call another one before finishing. Telling it to check its own coverage just makes it actually do that, instead of stopping the first time it has <em>something</em> to say.</p>

<p>One more thing worth keeping in mind as a system grows: each subagent should own a distinct, non-overlapping slice of the domain. Order status, pricing, and returns don’t overlap here, so there’s no risk of two subagents doing the same work twice. In a research-style system with more subagents and broader topics, that’s not automatic — partitioning the scope so subagents don’t duplicate each other’s work is its own design decision, the same way task decomposition is.</p>

<h2 id="consequences">Consequences</h2>

<p>Skipping these decisions doesn’t make the system simpler. It just hides the problems until they show up somewhere else.</p>

<ul>
  <li><strong>No decomposition rule</strong> -&gt; the coordinator delegates a simple question that never needed a subagent, wasting time and cost. Or it hands a subagent work it was never scoped to handle.</li>
  <li><strong>No coverage check</strong> -&gt; the coordinator stops as soon as it has <em>something</em> to say, not once it has addressed everything the user asked. Parts of a multi-topic question silently disappear from the answer, and nothing signals that they were dropped.</li>
  <li><strong>No context isolation</strong> -&gt; subagents start sharing context they shouldn’t. This defeats the whole point of splitting them apart. A bug in one topic can now affect an answer about a different topic, the same way it would in a single overloaded agent.</li>
  <li><strong>No clear communication protocol</strong> -&gt; the coordinator has to guess, from plain text, whether a subagent actually answered the question. A response that sounds fine but says nothing useful can pass as a success.</li>
  <li><strong>No sequencing rule</strong> -&gt; dependent subtasks run before the data they need is ready. Or independent subtasks run one after another for no reason, wasting time.</li>
  <li><strong>No error propagation rule</strong> -&gt; a subagent can fail quietly, and the coordinator still gives a confident final answer. The user can’t tell the difference between a correct answer and a wrong one built on missing data.</li>
  <li><strong>No aggregation rule</strong> -&gt; the “final answer” is just the subagent answers glued together, contradictions included. The user is left to sort it out.</li>
  <li><strong>No depth or cost control</strong> -&gt; a subagent that keeps spawning more subagents multiplies the cost and the time at every level, with no limit. A bill or a timeout catches the problem before the architecture does.</li>
</ul>

<p>These decisions don’t replace the ones from Part 1. Every subagent is still its own loop, and it still needs its own rules for stopping and handling errors. Orchestration adds a second layer on top of that. It doesn’t remove the first one.</p>]]></content><author><name>Istvan Dobrentei</name></author><category term="AI" /><category term="Claude" /><summary type="html"><![CDATA[Why one agent loaded with too many tools starts to break down, and how splitting it into a coordinator and focused subagents fixes it - task decomposition, context isolation, parallel vs. sequential execution, and error propagation across a real agentic system.]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="https://www.en.dobrenteiistvan.hu/assets/images/articles/coordinator-subagent.svg" /><media:content medium="image" url="https://www.en.dobrenteiistvan.hu/assets/images/articles/coordinator-subagent.svg" xmlns:media="http://search.yahoo.com/mrss/" /></entry><entry><title type="html">MQTT: The Protocol Designed for a World Where Things Fail</title><link href="https://www.en.dobrenteiistvan.hu/blog/mqtt.html" rel="alternate" type="text/html" title="MQTT: The Protocol Designed for a World Where Things Fail" /><published>2026-07-26T00:00:00+00:00</published><updated>2026-07-26T00:00:00+00:00</updated><id>https://www.en.dobrenteiistvan.hu/blog/mqtt</id><content type="html" xml:base="https://www.en.dobrenteiistvan.hu/blog/mqtt.html"><![CDATA[<p>Imagine thousands of sensors distributed across a factory, a fleet of delivery drones, or millions of smart meters in different locations. How can these devices communicate reliably when networks are slow, unreliable, and expensive? MQTT (Message Queuing Telemetry Transport) was created to solve exactly this problem.</p>

<h2 id="the-origin-story-of-mqtt">The origin story of MQTT</h2>

<p>The story of MQTT begins in the late 1990s, before the term IoT (Internet of Things) became popular. At that time, engineers were already trying to connect remote machines, sensors, and industrial equipment, but the available communication methods were not well suited for these environments.</p>

<p>Most people describe MQTT as a messaging protocol, but it was created because the physical world is unreliable. Instead of describing it feature by feature, I’ll explain it through the questions it was designed to answer. Before that, it helps to understand the three roles involved in every MQTT system.</p>

<h3 id="the-publisher">The publisher</h3>

<p>A publisher is a device or application that sends information. It publishes messages to a topic without knowing who, if anyone, will receive them.</p>

<p>For example: a temperature sensor may publish the current temperature every minute, or an EV (Electric Vehicle) charger may publish its charging status.</p>

<h3 id="the-subscriber">The subscriber</h3>

<p>A subscriber is a device or application that receives information. It subscribes to one or more topics and automatically receives every message published on those topics.</p>

<p>For example: a monitoring dashboard can subscribe to temperature readings from all sensors in a factory, while an alarm subscribes to the same data to detect overheating.</p>

<h3 id="the-broker">The broker</h3>

<p>The broker is the central communication hub. Every message sent by a publisher first arrives at the broker, which then forwards it to all subscribers interested in that topic.</p>

<p>The broker also handles many of MQTT’s reliability features, such as keeping track of connected clients, storing messages when needed, and detecting when a client disconnects unexpectedly.</p>

<h3 id="what-do-clients-and-brokers-look-like-in-practice">What do clients and brokers look like in practice?</h3>

<p>None of this is as abstract as it sounds. A “client” is just anything running MQTT client software. It can be an ESP32 microcontroller with a few kilobytes of firmware, a Raspberry Pi running Home Assistant, a smartphone app checking today’s solar production, or a few lines of code inside an industrial PLC. A commercial smart plug running Tasmota is a client. Home Assistant is a client too. It just happens to be one that subscribes to hundreds of topics and publishes commands back to many of them at once.</p>

<p>A “broker” is just as simple: it’s server software, usually a single process. <strong>Eclipse Mosquitto</strong> is the one most home labs reach for. It’s light enough to run on a Raspberry Pi, in a Docker container, or as a Home Assistant add-on, and you can mostly forget about it once it’s running. It comfortably handles thousands of messages a second on hardware that costs less than the charger it’s monitoring. At the other end of the scale, <strong>EMQX</strong> and <strong>HiveMQ</strong> are broker platforms built to run clustered across many servers. They can handle millions of connected clients at once — the kind of infrastructure a nationwide smart meter rollout would need. Cloud providers also sell brokers as a managed service, like <strong>AWS IoT Core</strong> or <strong>Azure IoT Hub</strong>, so a manufacturer never has to run one at all.</p>

<p>None of that changes the protocol underneath. A $3 microcontroller and a clustered enterprise broker are speaking exactly the same MQTT to each other. The roles are identical. Only the scale differs.</p>

<h3 id="one-client-multiple-roles">One client, multiple roles</h3>

<p>In MQTT, a client is not limited to a single role. The same client can both publish and subscribe.</p>

<p>For example, a smart thermostat publishes the current room temperature while also subscribing to commands that change the desired temperature.</p>

<p>In practice, MQTT has only two types of participants:</p>

<ul>
  <li>Clients, which can publish, subscribe, or do both</li>
  <li>The broker, which connects clients and manages the communications</li>
</ul>

<p>This simple architecture is one of the reasons MQTT scales so well. Publishers never need to know who receives their messages, and subscribers never need to know who produced them. The broker takes care of connecting the two.</p>

<p>MQTT actually implements a broader pattern called Publish/Subscribe (Pub/Sub). It is not something unique to MQTT. MQTT is simply one implementation of that pattern. The key idea behind it is <strong>decoupling</strong>. Publishers and subscribers do not know about each other. They only agree on a common “language” — topics or events — while a broker or message bus delivers the messages.</p>

<p>MQTT’s Publish/Subscribe model is closely related to other software patterns, like the Observer Pattern, common in object-oriented programming. An object (the subject) notifies all registered observers when its state changes. You’ll find it most often in GUI frameworks. You can find the same idea in Event-Driven Architecture, which applies the same pattern to the physical world. Instead of software services producing business events, devices produce events about the state of the real world. The architecture is the same: something happens, an event is published, and interested systems react.</p>

<p>Networks fail. Power disappears. Cellular coverage comes and goes. Sensors reboot. Messages get lost. Yet industrial automation, smart cities, connected vehicles and energy systems still need to operate.</p>

<h3 id="how-do-you-know-a-device-is-still-alive">How do you know a device is still alive?</h3>

<p>Imagine a temperature sensor installed on a remote pipeline. It may not send data every minute because the temperature hasn’t changed. If nothing arrives for an hour, is the temperature stable? Or has the device died?</p>

<p>MQTT answers this with the Keep Alive mechanism.</p>

<p>Rather than continuously transmitting data, the client periodically proves it is still connected. If the broker stops hearing from it within the agreed interval, the connection is considered lost.</p>

<h3 id="what-happens-when-a-device-suddenly-disappears">What happens when a device suddenly disappears?</h3>

<p>A factory robot loses power, a drone flies out of cellular coverage, an EV charger reboots. Nobody sends a goodbye message. How does the rest of the system know?</p>

<p>MQTT introduces the Last Will and Testament (LWT).</p>

<p>Before communication starts, the client leaves instructions with the broker:</p>

<p>“If I disappear unexpectedly, publish this message on my behalf.”</p>

<p>Other clients immediately know that the device went offline.</p>

<h3 id="what-happens-to-messages-while-the-device-is-offline">What happens to messages while the device is offline?</h3>

<p>Not every message has the same value. A temperature reading from five minutes ago may be useless. A payment confirmation must never disappear. MQTT allows applications to choose. Should messages be discarded or should the broker keep them until the client reconnects?</p>

<p>This is where persistent sessions become important. Instead of treating disconnections as failures, MQTT treats them as temporary interruptions.</p>

<h3 id="how-do-you-know-a-message-actually-arrived">How do you know a message actually arrived?</h3>

<p>A sensor publishes a reading. Did the broker receive it? Did the subscriber? On a shaky mobile network, packets get dropped mid-transmission all the time. MQTT doesn’t leave this to chance. It lets you choose, per message, how hard the protocol should work to make sure the message lands.</p>

<p>At QoS 0, a message is sent once and forgotten — fire and forget. Fine for a reading that will be superseded a second later anyway.</p>

<p>At QoS 1, the message is redelivered until the receiver acknowledges it — guaranteed to arrive, but possibly more than once.</p>

<p>At QoS 2, MQTT adds an extra handshake that guarantees the message arrives exactly once — no duplicates, no drops — at the cost of more round trips.</p>

<p>Nobody has to pick one level for the whole system. A pipeline pressure reading might travel at QoS 0. A valve-close command might travel at QoS 2. The protocol lets the importance of the message decide how much effort goes into delivering it.</p>

<h3 id="how-does-a-system-recover-after-failure">How does a system recover after failure?</h3>

<p>A client reconnects after being offline for ten minutes. What happened to it in that time?</p>

<p>If it connected with a persistent session, the broker was holding onto anything sent to it at QoS 1 or QoS 2 while it was gone. Those messages arrive the moment it comes back, in order. The client doesn’t need any special recovery logic. From its point of view, the outage just feels like a slightly longer wait.</p>

<p>If it connected with a clean session instead, none of that history follows it. It starts fresh, subscribes again, and only sees what gets published from that point forward.</p>

<p>Neither choice is “correct” on its own. A factory alarm system wants the messages it missed. A live GPS tracker mostly doesn’t care what happened while it was disconnected — it only cares where the vehicle is now. MQTT lets each client decide which kind of recovery it actually needs.</p>

<h3 id="why-wasnt-http-enough">Why wasn’t HTTP enough?</h3>

<p>HTTP is a request-response protocol: a client asks, a server answers. That model assumes the client knows who to ask, and that the server is reachable and listening right at that moment.</p>

<p>Neither assumption holds up well for a sensor network. A pipeline sensor doesn’t know which of several dashboards, alarms, or logging systems might care about its reading, and it shouldn’t have to know. If it had to open a connection and send a request to each one individually, adding a new subscriber would mean reconfiguring every publisher in the system.</p>

<p>HTTP also expects a server to be reachable on demand. Devices on a cellular or satellite link aren’t always reachable. Polling constantly to ask “did anything change?” burns bandwidth and battery, and most of those checks come back with nothing new anyway.</p>

<p>MQTT flips the relationship. A device doesn’t ask anyone anything. It just announces what it knows, and whoever is listening gets notified. Adding a new subscriber means one more client connecting to the broker. Nothing has to change on the publisher’s side.</p>

<h3 id="why-does-mqtt-still-matter-decades-later">Why does MQTT still matter, decades later?</h3>

<p>MQTT was designed in 1999 for oil pipelines and satellite links, about as far from a smart home as you can get. And yet it’s the same protocol quietly running inside Home Assistant, inside industrial SCADA systems, inside the EV charger in someone’s garage.</p>

<p>That’s not nostalgia. The problem MQTT solves was never really about the technology of any particular decade. Networks were unreliable in 1999, and they’re still unreliable now, just in new shapes: a Wi-Fi dropout instead of a satellite delay, a cheap microcontroller instead of a pipeline modem. The underlying pattern is simple: decouple publishers from subscribers, let a broker absorb the unreliability, and treat presence and disconnection as first-class events instead of edge cases. That pattern turned out to fit almost anything that produces data on and off and needs that data to reach more than one place.</p>

<p>That’s why a protocol old enough to predate the term “IoT” is still the obvious choice for a new EV charger deciding when to draw power from the sun instead of the grid.</p>]]></content><author><name>Istvan Dobrentei</name></author><category term="IoT" /><category term="MQTT" /><summary type="html"><![CDATA[Why MQTT, a protocol designed in 1999 for unreliable networks, still powers IoT today - publishers, subscribers, brokers, QoS levels, and why it handles disconnection better than HTTP.]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="https://www.en.dobrenteiistvan.hu/assets/images/articles/mqtt-pubsub.svg" /><media:content medium="image" url="https://www.en.dobrenteiistvan.hu/assets/images/articles/mqtt-pubsub.svg" xmlns:media="http://search.yahoo.com/mrss/" /></entry><entry><title type="html">Triathlon 2023</title><link href="https://www.en.dobrenteiistvan.hu/blog/triatlon-2023.html" rel="alternate" type="text/html" title="Triathlon 2023" /><published>2023-12-31T00:00:00+00:00</published><updated>2023-12-31T00:00:00+00:00</updated><id>https://www.en.dobrenteiistvan.hu/blog/triatlon-2023</id><content type="html" xml:base="https://www.en.dobrenteiistvan.hu/blog/triatlon-2023.html"><![CDATA[<p>After the COVID period, a new swimming pool was built in my city. Shortly after its opening, I came across an advertisement for a swimming coach.
I had wanted to learn how to swim properly for years, and I thought—this is a great opportunity.
The first lessons were truly memorable—I couldn’t even swim across the 50-meter pool. It felt like a real challenge, almost a fight for survival.
But I had strong motivation: sitting in front of a computer all day had left my neck weak and painful. I knew I needed a change.</p>

<p>Since February 1st, 2022, I’ve been swimming three to four times a week. I also joined the local triathlon club. Running, swimming, and cycling have become my lifestyle, and I regularly participate in sprint-distance competitions.
In 2023, I received “Most Motivated” recognition from the club for my dedication.
I draw a lot of inspiration from my teammates—I’m truly grateful to them all. And of course, the journey continues!</p>]]></content><author><name>Istvan Dobrentei</name></author><category term="Hobbies" /><category term="Triathlon" /><summary type="html"><![CDATA[After the COVID period, a new swimming pool was built in my city. Shortly after its opening, I came across an advertisement for a swimming coach. I had wanted to learn how to swim properly for years, and I thought—this is a great opportunity. The first lessons were truly memorable—I couldn’t even swim across the 50-meter pool. It felt like a real challenge, almost a fight for survival. But I had strong motivation: sitting in front of a computer all day had left my neck weak and painful. I knew I needed a change.]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="https://www.en.dobrenteiistvan.hu/assets/images/triatlon/triatlon-geac-2023.jpg" /><media:content medium="image" url="https://www.en.dobrenteiistvan.hu/assets/images/triatlon/triatlon-geac-2023.jpg" xmlns:media="http://search.yahoo.com/mrss/" /></entry><entry><title type="html">Isaszeg running 2015</title><link href="https://www.en.dobrenteiistvan.hu/blog/isaszeg-running-2015.html" rel="alternate" type="text/html" title="Isaszeg running 2015" /><published>2015-04-06T00:00:00+00:00</published><updated>2015-04-06T00:00:00+00:00</updated><id>https://www.en.dobrenteiistvan.hu/blog/isaszeg-running-2015</id><content type="html" xml:base="https://www.en.dobrenteiistvan.hu/blog/isaszeg-running-2015.html"><![CDATA[<p>As I had planned the year before, I returned in April 2015 to run again around the historic battlefield near Isaszeg. But this time, something unexpected and truly special happened.</p>

<p>To my surprise, I had the chance to run around the city with someone who plays a role in the animated battle reconstruction event — and who also happens to be a local resident. His name is István Verhás, and I’m incredibly grateful to him for guiding me through the area and sharing stories and insights from a local’s perspective.</p>

<p>Running with István made the experience even more meaningful. It wasn’t just a run — it became a journey through time, enriched by the personal connection and his deep knowledge of the area.</p>

<p>Many thanks to István for the guidance. It was an unforgettable experience and one of the highlights of my running journey so far.</p>]]></content><author><name>Istvan Dobrentei</name></author><category term="Hobbies" /><category term="Running" /><summary type="html"><![CDATA[As I had planned the year before, I returned in April 2015 to run again around the historic battlefield near Isaszeg. But this time, something unexpected and truly special happened.]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="https://www.en.dobrenteiistvan.hu/assets/images/futas/isaszegi_csata_futas_2015.jpg" /><media:content medium="image" url="https://www.en.dobrenteiistvan.hu/assets/images/futas/isaszegi_csata_futas_2015.jpg" xmlns:media="http://search.yahoo.com/mrss/" /></entry></feed>